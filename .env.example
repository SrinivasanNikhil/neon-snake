# Runtime server configuration
NODE_ENV=development
HOST=0.0.0.0
PORT=3000
ALLOWED_ORIGINS=http://localhost:3000
DATABASE_PATH=./data/neon-snake.sqlite

# Offline question generation only. Never expose this through VITE_* or Vite define.
# The deployed game server does not require this variable.
OPENAI_API_KEY=
OPENAI_QUESTION_MODEL=
