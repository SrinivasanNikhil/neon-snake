# Chapter 4 question review packet

Candidates: 20. Automated semantic audit: gpt-5.4 at 2026-08-13T16:20:38.740Z.

> The automated audit is advisory. A person must verify the source, SQL behavior, answer uniqueness, wording, and explanation before approval.

## Chapter checklist

- [ ] Every question was read in full.
- [ ] Every SQL result or constraint claim was independently checked.
- [ ] Every correct answer is uniquely defensible.
- [ ] Every source section supports the tested concept.
- [ ] Wording is original, accessible, and not copied from the book.
- [ ] The chapter has acceptable section and difficulty coverage.

## 1. ch04-ai-55c0973d-188-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a database with a 1:m relationship
- Concepts: ch04-foreign-key
- Kind/difficulty: sql-scenario, 2/5
- Automated audit: **pass**

Audit rationale: The item correctly tests foreign-key placement in a one-to-many design. Option A is uniquely correct, the distractors are plausible but wrong for clear reasons, and the explanation accurately links one-side and many-side key placement. Difficulty 2 is credible.

### Prompt

A bookstore tracks authors and books. One author can write many books, and each book has one author. Which table design correctly implements this one-to-many relationship?

| Option | Text |
| --- | --- |
| A ✓ | Store `author_id` in the `Book` table and keep author details in `Author` |
| B | Store `book_id` in the `Author` table and keep book details in `Book` |
| C | Store only `author_name` in the `Book` table and do not create an `Author` table |
| D | Store all author and book data in one table with repeated author rows |

Correct answer: **A — Store `author_id` in the `Book` table and keep author details in `Author`**

Explanation: In a one-to-many relationship, the key from the one side goes into the many-side table as a foreign key. Here, one author can relate to many books, so `author_id` belongs in `Book`. The other choices either place the key on the wrong side or repeat author facts instead of implementing the relationship properly.

Review concept: Place the one-side key in the many-side table to implement a one-to-many relationship.

Source: [The One-to-Many Relationship — Creating a database with a 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#creating-a-database-with-a-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-55c0973d-188-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 2. ch04-ai-f6e2b465-d26-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a database with a 1:m relationship
- Concepts: ch04-foreign-key
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: This is a straightforward and accurate test of foreign-key mapping. Only option A is correct, distractors are plausible misconceptions, and the explanation clearly states why the one-side key goes into the many-side table. Difficulty 2 is fitting.

### Prompt

A company stores departments and employees in two tables. Each employee belongs to exactly one department, and the department identifier should be stored so that each employee row points to its department. Which design best implements this relationship?

| Option | Text |
| --- | --- |
| A ✓ | Put the department identifier in the Employees table as a foreign key |
| B | Put the employee identifier in the Departments table as a foreign key |
| C | Create one separate table row for every employee-department pair |
| D | Store the department name only in the Employees table and leave it unmatched |

Correct answer: **A — Put the department identifier in the Employees table as a foreign key**

Explanation: In a one-to-many design, the key from the one side is stored in the many-side table. That lets each employee row reference exactly one department while many employees can share the same department value.

Review concept: Foreign-key mapping

Source: [The One-to-Many Relationship — Creating a database with a 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#creating-a-database-with-a-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-f6e2b465-d26-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 3. ch04-ai-f6e2b465-d26-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a database with a 1:m relationship
- Concepts: ch04-referential-integrity
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The statement of referential integrity is accurate, the assumption about a non-null foreign key is explicit, and only option A is defensible. The explanation is concise and correct. Difficulty 2 is appropriate.

### Prompt

Assume a child table has a non-null foreign key that references a parent table. What must be true for the child row to satisfy referential integrity?

| Option | Text |
| --- | --- |
| A ✓ | The foreign-key value must match an existing parent key value |
| B | The foreign-key value must be greater than every parent key value |
| C | The foreign-key value must be unique across all child rows |
| D | The foreign-key value must be generated automatically by the database |

Correct answer: **A — The foreign-key value must match an existing parent key value**

Explanation: For a non-null foreign key, referential integrity requires that the value match an existing key in the referenced parent table. It does not require the value to be the largest, unique in the child table, or automatically generated.

Review concept: A non-null foreign key must match an existing referenced key.

Source: [The One-to-Many Relationship — Creating a database with a 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#creating-a-database-with-a-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-f6e2b465-d26-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 4. ch04-ai-f6e2b465-d26-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a database with a 1:m relationship
- Concepts: ch04-referential-integrity
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The SQL inserts are valid as framed, the nullability assumption is explicitly stated, and option C is the only insert that violates referential integrity. The explanation correctly distinguishes valid parent matches from an invalid nonmatching key and allowed NULL. Difficulty 3 is fair.

### Prompt

A table `orders(customer_id, order_date)` has `customer_id` defined as a foreign key to `customers(customer_id)`. Assume `orders.customer_id` allows `NULL`, and `customers` currently contains only customer IDs 10 and 20. Which insert would violate referential integrity?

| Option | Text |
| --- | --- |
| A | ```sql<br>INSERT INTO orders(customer_id, order_date)<br>VALUES (10, '2026-01-15');<br>``` |
| B | ```sql<br>INSERT INTO orders(customer_id, order_date)<br>VALUES (20, '2026-01-16');<br>``` |
| C ✓ | ```sql<br>INSERT INTO orders(customer_id, order_date)<br>VALUES (30, '2026-01-17');<br>``` |
| D | ```sql<br>INSERT INTO orders(customer_id, order_date)<br>VALUES (NULL, '2026-01-18');<br>``` |

Correct answer: **C — ```sql
INSERT INTO orders(customer_id, order_date)
VALUES (30, '2026-01-17');
```**

Explanation: Because `customer_id` is a foreign key, any non-null value must match an existing `customers.customer_id`. Values 10 and 20 are valid because those parent rows exist, and `NULL` is allowed by the stated assumption. Value 30 has no matching parent row, so that insert violates referential integrity.

Review concept: A non-null foreign key must reference an existing parent row.

Source: [The One-to-Many Relationship — Creating a database with a 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#creating-a-database-with-a-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-f6e2b465-d26-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 5. ch04-ai-f6e2b465-d26-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a database with a 1:m relationship
- Concepts: ch04-foreign-key
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: The DDL and interpretation are correct. Option A accurately describes `employee.dept_id` as a foreign key storing the parent identifier in the many-side table, while the distractors are clearly wrong but still plausible enough. Difficulty 3 is acceptable.

### Prompt

Consider these tables:

```sql
CREATE TABLE department (
  dept_id INT PRIMARY KEY,
  dept_name VARCHAR(50)
);

CREATE TABLE employee (
  emp_id INT PRIMARY KEY,
  dept_id INT,
  emp_name VARCHAR(50),
  FOREIGN KEY (dept_id) REFERENCES department(dept_id)
);
```

Which statement best describes the role of `employee.dept_id`?

| Option | Text |
| --- | --- |
| A ✓ | It stores the one-side table's identifier in the many-side table as a foreign key |
| B | It is the primary key that uniquely identifies each department |
| C | It is a computed column that counts employees per department |
| D | It is a junction table that resolves a many-to-many relationship |

Correct answer: **A — It stores the one-side table's identifier in the many-side table as a foreign key**

Explanation: `department` is the one side and `employee` is the many side, because one department can have many employees. So `employee.dept_id` stores the department’s key value in the employee table as a foreign key, which is how a one-to-many relationship is implemented.

Review concept: Implement a one-to-many relationship by storing the one-side key in the many-side table.

Source: [The One-to-Many Relationship — Creating a database with a 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#creating-a-database-with-a-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-f6e2b465-d26-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 6. ch04-ai-f6e2b465-d26-5

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a database with a 1:m relationship
- Concepts: ch04-referential-integrity
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: The item correctly focuses on referential integrity as the rule preventing orphaned foreign-key references. Only option B directly supports the stated goal, and the explanation is accurate and instructive. Difficulty 3 is credible.

### Prompt

A sales database has `invoice(customer_id)` where `customer_id` is a foreign key to `customer(customer_id)`. The business wants to prevent invoices from referring to customers that do not exist. Which rule most directly supports that goal?

| Option | Text |
| --- | --- |
| A | Allow the foreign-key column to accept values that do not appear in the parent table |
| B ✓ | Require each non-null `customer_id` in `invoice` to match a `customer_id` in `customer` |
| C | Store the customer name instead of the customer key in the `invoice` table |
| D | Remove the foreign key and rely on application memory to track customers |

Correct answer: **B — Require each non-null `customer_id` in `invoice` to match a `customer_id` in `customer`**

Explanation: The stated goal is to prevent references to missing customers. Referential integrity does that by requiring each non-null foreign-key value to match an existing key in the parent table. The other options either allow bad references or avoid the key relationship entirely.

Review concept: Referential integrity prevents non-null foreign keys from pointing to missing parent rows.

Source: [The One-to-Many Relationship — Creating a database with a 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#creating-a-database-with-a-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-f6e2b465-d26-5 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 7. ch04-ai-5edcae3f-ded-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a two-table database
- Concepts: ch04-two-table-join
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: The SQL is correct, the query result is described accurately, and only option A matches an inner join on the foreign key. Distractors are plausible alternatives such as aggregation or outer join behavior. The explanation teaches row matching and exclusion of unmatched departments. Difficulty 2 is fair.

### Prompt

A company stores departments in `Department(dept_id, dept_name)` and employees in `Employee(emp_id, emp_name, dept_id)`, where `Employee.dept_id` references `Department.dept_id`. What does this query return?

```sql
SELECT d.dept_name, e.emp_name
FROM Department d
JOIN Employee e
  ON e.dept_id = d.dept_id
ORDER BY d.dept_name, e.emp_name;
```

| Option | Text |
| --- | --- |
| A ✓ | Each employee matched with the name of the department they belong to |
| B | One row per department, with employee names combined into a single field |
| C | Only departments that have no employees |
| D | All departments, even if they have no employees, with blank employee names |

Correct answer: **A — Each employee matched with the name of the department they belong to**

Explanation: The join matches each employee row to the department row with the same department id. That produces one result row per matching employee-department pair, showing each employee with their department name. Because this is an inner join, departments with no employees are excluded.

Review concept: Two-table joins

Source: [The One-to-Many Relationship — Querying a two-table database](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#querying-a-two-table-database)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-5edcae3f-ded-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 8. ch04-ai-5edcae3f-ded-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a two-table database
- Concepts: ch04-group-having
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: This accurately assesses GROUP BY with HAVING. The query and explanation are correct, only option A is defensible, and the distractors reflect common confusion about row-level versus group-level filtering. Difficulty 3 is reasonable.

### Prompt

A sales database has `OrderLine(order_id, product_id, qty)`. Consider this query:

```sql
SELECT product_id, COUNT(*) AS line_count
FROM OrderLine
GROUP BY product_id
HAVING COUNT(*) > 10;
```

What does this query return?

| Option | Text |
| --- | --- |
| A ✓ | Products whose number of order lines is greater than 10 |
| B | Individual order lines where `COUNT(*)` is greater than 10 |
| C | Every product, with a label showing whether its count is over 10 |
| D | Rows filtered before grouping so only products already above 10 are counted |

Correct answer: **A — Products whose number of order lines is greater than 10**

Explanation: `GROUP BY product_id` forms one group per product, `COUNT(*)` counts how many order-line rows are in each group, and `HAVING COUNT(*) > 10` keeps only the groups whose count exceeds 10. So the result is the set of products appearing in more than 10 order lines.

Review concept: Use GROUP BY and HAVING to filter grouped results by aggregate values.

Source: [The One-to-Many Relationship — Querying a two-table database](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#querying-a-two-table-database)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-5edcae3f-ded-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 9. ch04-ai-5edcae3f-ded-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a two-table database
- Concepts: ch04-control-break
- Kind/difficulty: concept, 2/5
- Automated audit: **needs-edit** — poor-explanation, weak-distractor

Audit rationale: The keyed answer is correct, but the stem says the report prints a subtotal each time the customer name changes. In this context, the subtotal is more naturally by some detail field grouped under customer, not by customer itself, so the explanation is somewhat loose and does not clearly connect the break field to the subtotaled detail values. Also, distractors B-D are not especially plausible reporting-technique alternatives, making the item easier than stated.

Suggested fix: Revise the stem to make the grouping clearer, e.g., 'A report lists invoice lines sorted by customer name and prints a subtotal whenever the customer name changes.' Improve distractors with more plausible report/query terms, and expand the explanation to clarify that the control field is the sorted attribute whose value change triggers the subtotal.

### Prompt

A report lists customers in alphabetical order and prints a subtotal each time the customer name changes. What reporting technique is being used?

| Option | Text |
| --- | --- |
| A ✓ | Control-break reporting |
| B | Cartesian joining |
| C | Normalization |
| D | Referential checking |

Correct answer: **A — Control-break reporting**

Explanation: A control-break report groups sorted rows by a break field and prints summaries whenever the value changes. Subtotals by customer name are a classic control-break pattern.

Review concept: Control-break reporting

Source: [The One-to-Many Relationship — Querying a two-table database](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#querying-a-two-table-database)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-5edcae3f-ded-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 10. ch04-ai-5edcae3f-ded-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a two-table database
- Concepts: ch04-two-table-join
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The stem and options align, the correct SQL join is unambiguous, and the distractors are plausible wrong queries. The explanation correctly distinguishes a proper join, missing join, wrong join condition, and Cartesian product. Difficulty 3 is appropriate.

### Prompt

A bookstore tracks authors in `Author(author_id, author_name)` and books in `Book(book_id, title, author_id)`. Every book has exactly one author. Which query correctly returns each book title with its author name?

| Option | Text |
| --- | --- |
| A ✓ | ```sql<br>SELECT b.title, a.author_name<br>FROM Book b<br>JOIN Author a<br>  ON b.author_id = a.author_id;<br>``` |
| B | ```sql<br>SELECT title, author_name<br>FROM Book;<br>``` |
| C | ```sql<br>SELECT b.title, a.author_name<br>FROM Book b<br>JOIN Author a<br>  ON b.book_id = a.author_id;<br>``` |
| D | ```sql<br>SELECT b.title, a.author_name<br>FROM Book b, Author a;<br>``` |

Correct answer: **A — ```sql
SELECT b.title, a.author_name
FROM Book b
JOIN Author a
  ON b.author_id = a.author_id;
```**

Explanation: A two-table join should match the foreign key in `Book` to the referenced key in `Author`, which is `b.author_id = a.author_id`. Option A does that correctly. Option B does not join to `Author` at all, option C joins on unrelated columns, and option D creates a Cartesian product because it has no join condition.

Review concept: Join related tables by matching a foreign key to its referenced key.

Source: [The One-to-Many Relationship — Querying a two-table database](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#querying-a-two-table-database)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-5edcae3f-ded-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 11. ch04-ai-5edcae3f-ded-5

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a two-table database
- Concepts: ch04-group-having
- Kind/difficulty: sql-reading, 4/5
- Automated audit: **pass**

Audit rationale: The SQL is correct, assumptions are explicit, and the keyed answer precisely matches the effect of WHERE, GROUP BY, COUNT(DISTINCT), and HAVING. Distractors target realistic misunderstandings. Difficulty 4 is acceptable though on the easier side for advanced beginners.

### Prompt

A clinic stores visits in `Visit(doctor_id, patient_id, visit_date)`. Consider this query:

```sql
SELECT doctor_id, COUNT(DISTINCT patient_id) AS patient_count
FROM Visit
WHERE visit_date >= '2026-06-01' AND visit_date < '2026-07-01'
GROUP BY doctor_id
HAVING COUNT(DISTINCT patient_id) >= 5;
```

What does this query return?

| Option | Text |
| --- | --- |
| A ✓ | Doctors who saw at least 5 distinct patients during June 2026 |
| B | Doctors whose total number of visits in all months is at least 5 |
| C | One row per visit in June 2026, with repeated doctors removed |
| D | Doctors who saw at least 5 patients, counting the same patient multiple times |

Correct answer: **A — Doctors who saw at least 5 distinct patients during June 2026**

Explanation: The `WHERE` clause limits rows to June 2026 before grouping. `GROUP BY doctor_id` creates one group per doctor, `COUNT(DISTINCT patient_id)` counts unique patients within each doctor’s June visits, and `HAVING ... >= 5` keeps only doctors meeting that threshold.

Review concept: Filter grouped results after aggregation with HAVING.

Source: [The One-to-Many Relationship — Querying a two-table database](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#querying-a-two-table-database)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-5edcae3f-ded-5 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 12. ch04-ai-55c0973d-188-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Relationships
- Concepts: ch04-one-to-many
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: The stem, options, and keyed answer align cleanly. Exactly one option describes a one-to-many relationship correctly, distractors are plausible misconceptions, and the explanation accurately teaches the parent/child-side idea. Difficulty 1 is appropriate.

### Prompt

A company keeps one record for each department and many records for the employees in that department. Which description best matches this relationship?

| Option | Text |
| --- | --- |
| A | Each employee can belong to many departments, and each department can belong to many employees |
| B ✓ | One department can be associated with many employees, while each employee belongs to one department |
| C | Each department must be split into multiple tables because it has many employees |
| D | The employee table should store one row for every department |

Correct answer: **B — One department can be associated with many employees, while each employee belongs to one department**

Explanation: In a one-to-many relationship, one row on the parent side can relate to many rows on the child side. Here, a department can have many employees, and each employee is linked to one department.

Review concept: Identify a one-to-many relationship.

Source: [The One-to-Many Relationship — Relationships](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#relationships)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-55c0973d-188-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 13. ch04-ai-55c0973d-188-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Relationships
- Concepts: ch04-redundancy
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: This is factually correct and well aligned to the redundancy concept. Option A is the only defensible answer, the distractors reflect common denormalization mistakes, and the explanation clearly states why the city belongs with the customer entity. Difficulty 2 is reasonable.

### Prompt

A sales app stores each invoice with the customer’s city in every invoice row. If the customer moves, many old rows would need updates. Which design choice best reduces this redundancy?

| Option | Text |
| --- | --- |
| A ✓ | Keep the city only in the customer table and reference the customer from each invoice |
| B | Duplicate the city in every invoice row so queries are faster |
| C | Replace the customer identifier with the customer city |
| D | Store the city once per invoice line item and once per invoice header |

Correct answer: **A — Keep the city only in the customer table and reference the customer from each invoice**

Explanation: When a fact describes its own entity, storing it once there avoids repeating the same data across many related rows. The customer city belongs with the customer, while invoices can point to that customer.

Review concept: Move repeated facts to the entity they describe.

Source: [The One-to-Many Relationship — Relationships](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#relationships)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-55c0973d-188-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 14. ch04-ai-55c0973d-188-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Relationships
- Concepts: ch04-one-to-many, ch04-redundancy
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The question combines one-to-many modeling and redundancy avoidance effectively. Only option B satisfies both conditions, and the explanation teaches both entity separation and foreign-key linkage. Difficulty 2 is appropriate.

### Prompt

A clinic stores patients and appointments. A patient may have many appointments, but each appointment belongs to one patient. The clinic also wants to avoid storing the patient’s phone number repeatedly. Which design best fits both goals?

| Option | Text |
| --- | --- |
| A | Keep `phone_number` in the `Appointment` table because appointments are queried more often |
| B ✓ | Create a `Patient` table with `phone_number`, and store `patient_id` in the `Appointment` table |
| C | Create one table that repeats patient data in every appointment row |
| D | Store `phone_number` in both tables so the records are easier to read |

Correct answer: **B — Create a `Patient` table with `phone_number`, and store `patient_id` in the `Appointment` table**

Explanation: This design separates patient facts from appointment facts and connects them with `patient_id`. That matches a one-to-many relationship because one patient can have many appointments, and it avoids repeating the same phone number in multiple appointment rows.

Review concept: Use separate entities to avoid repeated facts in a one-to-many design.

Source: [The One-to-Many Relationship — Relationships](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#relationships)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-55c0973d-188-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 15. ch04-ai-55c0973d-188-5

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Relationships
- Concepts: ch04-one-to-many, ch04-redundancy
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The item is conceptually sound, with one clearly correct option and plausible distractors based on wrong-side key placement or redundancy. The explanation correctly emphasizes storing magazine facts once and relating articles to magazines. Difficulty 2 fits.

### Prompt

A publisher stores magazines and articles. Each article belongs to exactly one magazine, but a magazine has many articles. The team also wants to prevent the same magazine facts from being copied into every article row. Which rule best fits this design?

| Option | Text |
| --- | --- |
| A ✓ | Keep magazine details in a separate `Magazine` table and relate each article to its magazine |
| B | Store `article_id` in the `Magazine` table so each magazine points to one article |
| C | Copy magazine title, issue date, and editor into every article row |
| D | Use one table for both entities and leave magazine columns blank in many rows |

Correct answer: **A — Keep magazine details in a separate `Magazine` table and relate each article to its magazine**

Explanation: Magazine facts describe a separate entity, so they should be stored once in a `Magazine` table rather than repeated in every article row. Each article can then be related to its magazine, which matches the one-to-many structure and reduces redundancy.

Review concept: Separate repeated facts into their own entity in a one-to-many relationship.

Source: [The One-to-Many Relationship — Relationships](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#relationships)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-55c0973d-188-5 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 16. ch04-ai-fd6a6239-c9a-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Subqueries and views
- Concepts: ch04-subqueries-views
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: This is accurate and aligned to the source’s treatment of views as reusable virtual tables. Option A is uniquely correct, distractors are plausible alternatives, and the explanation is clear. Difficulty 2 is suitable.

### Prompt

A company often needs the same filtered list of active products in several reports. Which approach best lets the team reuse that result as if it were a table, without storing duplicate data in a new base table?

| Option | Text |
| --- | --- |
| A ✓ | Create a view based on the filtering query |
| B | Create a trigger that runs the filtering query |
| C | Create a temporary table and manually refresh it each time |
| D | Add another copy of the product rows to a separate table |

Correct answer: **A — Create a view based on the filtering query**

Explanation: A view saves a query definition and presents its result through a table-like interface. That makes the filtered result reusable without duplicating the underlying data in another base table.

Review concept: Subqueries can isolate an intermediate result, while a view gives a saved query result a reusable virtual-table interface.

Source: [The One-to-Many Relationship — Subqueries and views](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#subqueries)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-fd6a6239-c9a-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 17. ch04-ai-fd6a6239-c9a-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Subqueries and views
- Concepts: ch04-subqueries-views
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: The item correctly describes how a view is treated in query use. Only option B is defensible, the distractors are plausible misunderstandings, and the explanation teaches the virtual-table idea accurately. Difficulty 2 is appropriate.

### Prompt

Assume `RecentOrders` is a view defined from a query. What does a database typically treat `RecentOrders` as when you use it in another `SELECT` statement?

```sql
SELECT order_id
FROM RecentOrders;
```

| Option | Text |
| --- | --- |
| A | A permanent copy of the rows stored separately from the base tables |
| B ✓ | A virtual table produced by the saved query definition |
| C | A trigger that executes before the query runs |
| D | A foreign key relationship between tables |

Correct answer: **B — A virtual table produced by the saved query definition**

Explanation: A view behaves like a virtual table. When referenced in a query, the database uses the saved query definition to produce the result, rather than treating it as a separately stored copy of the data.

Review concept: Subqueries can isolate an intermediate result, while a view gives a saved query result a reusable virtual-table interface.

Source: [The One-to-Many Relationship — Subqueries and views](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#subqueries)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-fd6a6239-c9a-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 18. ch04-ai-fd6a6239-c9a-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Subqueries and views
- Concepts: ch04-subqueries-views
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The SQL and logic are correct, and the stem explicitly provides the needed assumption that `CustomerTotals` already contains one row per customer. Option A is the only query that uses a subquery appropriately to compare against the overall average. The explanation is strong and pedagogically useful. Difficulty 3 is appropriate.

### Prompt

A report needs the customers whose total spending is greater than the average customer total. Assume `CustomerTotals(customer_id, total_spent)` already contains one row per customer.

Which SQL statement best uses a subquery as an intermediate result to meet this requirement?

| Option | Text |
| --- | --- |
| A ✓ | SELECT customer_id, total_spent<br>FROM CustomerTotals<br>WHERE total_spent > (<br>  SELECT AVG(total_spent)<br>  FROM CustomerTotals<br>); |
| B | SELECT customer_id, total_spent<br>FROM CustomerTotals<br>WHERE AVG(total_spent) > total_spent; |
| C | SELECT customer_id, total_spent<br>FROM CustomerTotals<br>GROUP BY customer_id, total_spent<br>HAVING total_spent > AVG(total_spent); |
| D | CREATE VIEW HighSpenders AS<br>SELECT customer_id, total_spent<br>FROM CustomerTotals; |

Correct answer: **A — SELECT customer_id, total_spent
FROM CustomerTotals
WHERE total_spent > (
  SELECT AVG(total_spent)
  FROM CustomerTotals
);**

Explanation: Option A is correct because the subquery computes one intermediate value: the average `total_spent` across all customers in `CustomerTotals`. The outer query then returns only those rows whose `total_spent` is greater than that overall average.

Option B is not valid SQL in this context because `AVG(total_spent)` is an aggregate and cannot be used directly in `WHERE` without being produced by a subquery or another valid aggregate context. Option C is valid SQL, but it is the wrong logic: after grouping by `customer_id, total_spent`, each group contains rows with the same `total_spent`, so `AVG(total_spent)` within each group equals that same `total_spent`, making `total_spent > AVG(total_spent)` never true. It does not compare each customer to the overall average. Option D creates a view of all rows but does not perform the required filtering against the average.

Review concept: Subqueries can isolate an intermediate result, while a view gives a saved query result a reusable virtual-table interface.

Source: [The One-to-Many Relationship — Subqueries and views](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#subqueries)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-fd6a6239-c9a-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 19. ch04-ai-fd6a6239-c9a-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Subqueries and views
- Concepts: ch04-subqueries-views
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: This is a clean subquery-reading item. The inner query’s role is correctly identified as producing an intermediate result for the outer filter, and the distractors are plausible but clearly incorrect. The explanation is concise and accurate. Difficulty 3 fits.

### Prompt

Consider this query:

```sql
SELECT employee_id
FROM Employees
WHERE department_id IN (
  SELECT department_id
  FROM Departments
  WHERE region = 'West'
);
```

What is the role of the inner `SELECT` statement?

| Option | Text |
| --- | --- |
| A | It creates a reusable virtual table that is stored in the database |
| B ✓ | It produces an intermediate result used by the outer query to filter rows |
| C | It defines the primary key for `Employees` |
| D | It changes the join cardinality between the two tables |

Correct answer: **B — It produces an intermediate result used by the outer query to filter rows**

Explanation: The inner query is a subquery. Its job is to compute an intermediate set of department IDs, and the outer query uses that result to filter employees.

Review concept: Subqueries can isolate an intermediate result, while a view gives a saved query result a reusable virtual-table interface.

Source: [The One-to-Many Relationship — Subqueries and views](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#subqueries)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-fd6a6239-c9a-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 20. ch04-ai-fd6a6239-c9a-5

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Subqueries and views
- Concepts: ch04-subqueries-views
- Kind/difficulty: sql-scenario, 4/5
- Automated audit: **pass**

Audit rationale: The stem clearly asks for a reusable, stable query interface, and a view is the single best answer. Distractors are plausible but incorrect, and the explanation accurately ties the choice to virtual-table reuse without duplication. Difficulty 4 is acceptable.

### Prompt

A retailer needs a stable interface for a complicated query that lists each order with its customer name and current status. Multiple analysts will query this result, and you want them to use the same definition without rewriting the query. What is the best design choice?

| Option | Text |
| --- | --- |
| A ✓ | Save the query as a view and let analysts query the view |
| B | Copy the result into a new permanent table every morning |
| C | Replace the query with a self-join on the same table |
| D | Move the query into a foreign key constraint |

Correct answer: **A — Save the query as a view and let analysts query the view**

Explanation: A view is appropriate when many users need the same query result through a reusable interface. It avoids repeating the query text and presents the result like a virtual table, while the underlying data remains in the base tables.

Review concept: Subqueries can isolate an intermediate result, while a view gives a saved query result a reusable virtual-table interface.

Source: [The One-to-Many Relationship — Subqueries and views](https://www.richardtwatson.com/open/Reader/_book/204-onetomany.html#subqueries)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch04-ai-fd6a6239-c9a-5 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

