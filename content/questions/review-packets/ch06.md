# Chapter 6 question review packet

Candidates: 20. Automated semantic audit: gpt-5.4 at 2026-08-13T16:21:11.709Z.

> The automated audit is advisory. A person must verify the source, SQL behavior, answer uniqueness, wording, and explanation before approval.

## Chapter checklist

- [ ] Every question was read in full.
- [ ] Every SQL result or constraint claim was independently checked.
- [ ] Every correct answer is uniquely defensible.
- [ ] Every source section supports the tested concept.
- [ ] Wording is original, accessible, and not copied from the book.
- [ ] The chapter has acceptable section and difficulty coverage.

## 1. ch06-ai-d4aba820-39f-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a one-to-one relationship
- Concepts: ch06-one-to-one-mapping
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The foreign-key placement logic is correct and matches the chapter concept well. Exactly one answer is defensible, distractors are plausible, and the explanation appropriately ties mandatory participation and uniqueness to the design. Difficulty 3 is credible.

### Prompt

A hospital stores patients and insurance cards. Business rules: each patient may have zero or one insurance card, and each insurance card must belong to exactly one patient. Which design best matches this one-to-one relationship while keeping the card's foreign key non-null?

| Option | Text |
| --- | --- |
| A ✓ | Store patient_id in InsuranceCard, declare it NOT NULL, and add a UNIQUE constraint on patient_id |
| B | Store insurance_card_id in Patient, declare it NOT NULL, and add a UNIQUE constraint on insurance_card_id |
| C | Store patient_id in Patient, allow it to be NULL, and add no uniqueness constraint |
| D | Create a separate junction table with patient_id and insurance_card_id as a many-to-many mapping |

Correct answer: **A — Store patient_id in InsuranceCard, declare it NOT NULL, and add a UNIQUE constraint on patient_id**

Explanation: Option A is correct because the mandatory side is InsuranceCard: every card must belong to a patient. Placing patient_id in InsuranceCard and declaring it NOT NULL enforces that requirement, and making patient_id UNIQUE prevents more than one card from being linked to the same patient. Option B would incorrectly force every patient to have a card. Option C neither places the foreign key appropriately nor enforces one-to-one uniqueness. Option D models a many-to-many relationship, which does not match the business rule.

Review concept: Choosing foreign-key placement

Source: [One-to-One and Recursive Relationships — Mapping a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-d4aba820-39f-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 2. ch06-ai-e918cf74-766-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a one-to-one relationship
- Concepts: ch06-one-to-one-mapping
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Correct one-to-one mapping question with clear mandatory side and null-minimization rationale. One answer is best, distractors are plausible, and the explanation is instructive. Difficulty 2 is fair.

### Prompt

A company has two tables, `Employee` and `CompanyCarAssignment`. Each employee may have at most one assigned car, and every assignment row must belong to exactly one employee. Which foreign-key placement best fits this design while avoiding unnecessary nulls?

| Option | Text |
| --- | --- |
| A ✓ | Put `employee_id` in `CompanyCarAssignment`, and make it `NOT NULL` and `UNIQUE`. |
| B | Put `car_assignment_id` in `Employee`, and make it `UNIQUE`. |
| C | Put foreign keys in both tables so each row points to the other. |
| D | Store the relationship in neither table and rely on application code. |

Correct answer: **A — Put `employee_id` in `CompanyCarAssignment`, and make it `NOT NULL` and `UNIQUE`.**

Explanation: `CompanyCarAssignment` is the dependent table because every assignment must belong to one employee. Making `employee_id` `NOT NULL` enforces that mandatory participation, and making it `UNIQUE` ensures no employee appears in more than one assignment row. This placement also avoids a mostly null assignment reference in `Employee` for employees who do not have a car.

Review concept: Choosing foreign-key placement

Source: [One-to-One and Recursive Relationships — Mapping a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-e918cf74-766-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 3. ch06-ai-e918cf74-766-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a one-to-one relationship
- Concepts: ch06-one-to-one-mapping
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: Well-constructed mapping question. Option A is best because it both preserves the rule and minimizes nulls. The explanation properly distinguishes between a merely workable design and the best design under the stated objective. Difficulty 3 is appropriate.

### Prompt

Suppose a database is modeling an optional one-to-one relationship between `Person(person_id PK, ...)` and `Passport(passport_id PK, ...)`. A person may exist before receiving a passport, but every passport must belong to a person. Which design best avoids unnecessary NULLs while preserving that rule?

| Option | Text |
| --- | --- |
| A ✓ | Store `person_id` in `Passport` as a foreign key, declare it NOT NULL, and make it UNIQUE |
| B | Store `passport_id` in `Person` as a nullable foreign key and make it UNIQUE |
| C | Store foreign keys in both tables and declare both NOT NULL |
| D | Create no foreign key and match rows later using application code |

Correct answer: **A — Store `person_id` in `Passport` as a foreign key, declare it NOT NULL, and make it UNIQUE**

Explanation: Option A is correct because the mandatory participation is on the Passport side: every passport must belong to a person, but a person may have no passport yet. Putting person_id in Passport as NOT NULL enforces that every passport references a person, and UNIQUE enforces that no person is linked to more than one passport. This choice also avoids unnecessary NULLs in Person for people who do not yet have passports. Option B can support the relationship, but it stores many NULL passport_id values for people without passports, so it does not minimize unnecessary NULLs. Option C creates a dependency cycle and forces both rows to exist immediately. Option D does not enforce the relationship in the database.

Review concept: Choosing foreign-key placement

Source: [One-to-One and Recursive Relationships — Mapping a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-e918cf74-766-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 4. ch06-ai-e918cf74-766-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a one-to-one relationship
- Concepts: ch06-cycle-risk
- Kind/difficulty: concept, 4/5
- Automated audit: **pass**

Audit rationale: Accurate treatment of cyclic mandatory foreign keys. One answer is clearly correct, distractors are plausible misconceptions, and the explanation teaches the insertion-order problem directly. Difficulty 4 is acceptable.

### Prompt

Two tables each require a mandatory foreign key to the other: `A` must reference exactly one `B`, and `B` must reference exactly one `A`. What is the main practical problem with this design when inserting new rows?

| Option | Text |
| --- | --- |
| A ✓ | Either row may be impossible to insert first because each one needs the other to already exist. |
| B | The database will always automatically convert the relationship into many-to-many. |
| C | The foreign keys will be treated as optional, so no constraint problem occurs. |
| D | The tables must be merged into one table, or the design is invalid. |

Correct answer: **A — Either row may be impossible to insert first because each one needs the other to already exist.**

Explanation: Mutual mandatory references create a cycle. If each row needs the other to exist first, there is no straightforward insertion order that satisfies both constraints at once.

Review concept: Constraint cycles

Source: [One-to-One and Recursive Relationships — Mapping a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-e918cf74-766-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 5. ch06-ai-e918cf74-766-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a one-to-one relationship
- Concepts: ch06-cycle-risk
- Kind/difficulty: sql-scenario, 5/5
- Automated audit: **pass**

Audit rationale: Strong scenario question with correct diagnosis of the dependency cycle. The schema and business rule align, exactly one answer is defensible, and the explanation is clear and instructive. Difficulty 5 is reasonable because it requires reading schema details and understanding mutual mandatory references.

### Prompt

A schema has:

`Account(account_id PK, primary_contact_id FK NOT NULL UNIQUE)`
`Contact(contact_id PK, account_id FK NOT NULL UNIQUE)`

Each account must have exactly one contact, and each contact must belong to exactly one account. A developer wants to insert a new account and its contact in separate statements without disabling constraints. What is the best explanation of why this is difficult?

| Option | Text |
| --- | --- |
| A ✓ | Each insert would violate a mandatory foreign key unless the other row already exists, so the two tables form a dependency cycle |
| B | MySQL cannot enforce foreign keys when both tables use primary keys |
| C | A one-to-one relationship cannot be represented with foreign keys at all |
| D | The problem is only that the foreign keys are not indexed |

Correct answer: **A — Each insert would violate a mandatory foreign key unless the other row already exists, so the two tables form a dependency cycle**

Explanation: Option A is correct because both foreign keys are mandatory, so neither row can be inserted first without referencing a row that does not yet exist. The UNIQUE constraints help enforce the one-to-one cardinality, but they do not solve the insertion-order problem. The real difficulty is the circular dependency between Account and Contact. Option B is false because foreign keys can reference primary keys. Option C is false because one-to-one relationships can be mapped with foreign keys plus uniqueness. Option D is unrelated to the immediate constraint cycle.

Review concept: Constraint cycles

Source: [One-to-One and Recursive Relationships — Mapping a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-e918cf74-766-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 6. ch06-ai-f61cb28c-347-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a recursive one-to-many relationship
- Concepts: ch06-recursive-foreign-key
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Correct and clear self-referential foreign-key question. One answer is clearly best, distractors are plausible, and the explanation accurately contrasts normalized design with poor alternatives. Difficulty 1 is suitable.

### Prompt

A company stores employees in one table: `employees(employee_id, employee_name, manager_id)`. Each employee may have a manager, and the manager is also an employee in the same table. Which design best represents this relationship?

| Option | Text |
| --- | --- |
| A ✓ | Add a manager_id column that references employees.employee_id in the same table |
| B | Create a separate managers table and copy every employee into it if they manage someone |
| C | Store the manager's name only as text so no foreign key is needed |
| D | Use a foreign key from employees.employee_id to managers.manager_id |

Correct answer: **A — Add a manager_id column that references employees.employee_id in the same table**

Explanation: Option A is correct because a recursive one-to-many relationship is represented with a self-referential foreign key. Here, manager_id stores the employee_id of another row in the same employees table. Option B duplicates data unnecessarily. Option C does not preserve referential integrity. Option D refers to a managers table that is not part of the stated design.

Review concept: Self-referential foreign keys

Source: [One-to-One and Recursive Relationships — Mapping a recursive one-to-many relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-recursive-one-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-f61cb28c-347-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 7. ch06-ai-f61cb28c-347-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a recursive one-to-many relationship
- Concepts: ch06-recursive-foreign-key
- Kind/difficulty: sql-scenario, 2/5
- Automated audit: **pass**

Audit rationale: The question correctly identifies NULL as the representation of no parent in an optional self-referential foreign key. Distractors are plausible bad patterns. Explanation is accurate and useful. Difficulty 2 fits.

### Prompt

A table departments(dept_id, dept_name, parent_dept_id) stores a hierarchical organization chart. parent_dept_id is a foreign key to departments.dept_id. Which row represents a top-level department with no parent?

| Option | Text |
| --- | --- |
| A ✓ | (10, 'Sales', NULL) |
| B | (10, 'Sales', 10) |
| C | (10, 'Sales', '') |
| D | (10, 'Sales', 0) |

Correct answer: **A — (10, 'Sales', NULL)**

Explanation: A top-level row has no parent, so the self-referencing foreign-key column should be null if the design allows optional parentage. The other values either point to itself, use an empty string, or use an arbitrary number instead of representing no parent.

Review concept: Self-referential foreign keys

Source: [One-to-One and Recursive Relationships — Mapping a recursive one-to-many relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-recursive-one-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-f61cb28c-347-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 8. ch06-ai-f61cb28c-347-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Mapping a recursive one-to-many relationship
- Concepts: ch06-recursive-foreign-key
- Kind/difficulty: sql-scenario, 4/5
- Automated audit: **pass**

Audit rationale: The keyed answer is correct: a self-referencing predecessor_id column models a recursive one-to-many pattern because many products may share the same predecessor. Distractors reflect common confusions, and the explanation resolves them well. Difficulty 4 is acceptable.

### Prompt

A product table has columns product_id, product_name, and predecessor_id. Each product can have at most one predecessor, and that predecessor must be another row in the same table. If a product has no predecessor, predecessor_id is NULL. Which statement is true about this design?

| Option | Text |
| --- | --- |
| A ✓ | It is a recursive one-to-many relationship mapped with a foreign key back to the same table |
| B | It is a many-to-many relationship that requires a bridge table |
| C | It is a one-to-one relationship because each product has at most one predecessor |
| D | It cannot be enforced with a foreign key because self-references are impossible |

Correct answer: **A — It is a recursive one-to-many relationship mapped with a foreign key back to the same table**

Explanation: A row that can point to another row in the same table through a foreign key is a recursive relationship. Because each product can reference at most one predecessor, the mapping is recursive one-to-many: many products may share the same predecessor, while each product points to at most one predecessor.

Review concept: Self-referential foreign keys

Source: [One-to-One and Recursive Relationships — Mapping a recursive one-to-many relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#mapping-a-recursive-one-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-f61cb28c-347-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 9. ch06-ai-d4aba820-39f-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Modeling a one-to-one relationship
- Concepts: ch06-one-to-one
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Accurate, unambiguous basic cardinality question with one clear answer. Distractors are appropriate for introductory level. Difficulty 1 fits.

### Prompt

Two tables represent people and passports. Each passport belongs to exactly one person, and each person can have at most one passport. Which relationship description best fits this situation?

| Option | Text |
| --- | --- |
| A ✓ | One-to-one |
| B | One-to-many |
| C | Many-to-many |
| D | Recursive one-to-many |

Correct answer: **A — One-to-one**

Explanation: Each row on either side can match at most one row on the other side, so the relationship is one-to-one.

Review concept: Recognizing one-to-one cardinality

Source: [One-to-One and Recursive Relationships — Modeling a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-d4aba820-39f-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 10. ch06-ai-d4aba820-39f-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Modeling a one-to-one relationship
- Concepts: ch06-relationship-labels
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Good chapter fit and concept alignment. One correct answer, plausible distractors, and an explanation that teaches the need for relationship labels when the same entities are connected in multiple ways. Difficulty 2 is appropriate.

### Prompt

A company has two different relationships between the same pair of entities, Employee and Department: one for where an employee works, and one for which department manages the employee record. Why are relationship labels useful here?

| Option | Text |
| --- | --- |
| A ✓ | They clarify which relationship is meant when the same entities are connected in more than one way |
| B | They eliminate the need for foreign keys |
| C | They automatically enforce uniqueness |
| D | They turn a one-to-one relationship into a recursive relationship |

Correct answer: **A — They clarify which relationship is meant when the same entities are connected in more than one way**

Explanation: When the same entities participate in multiple relationships, labels distinguish the different meanings so the model is unambiguous.

Review concept: Using relationship descriptors to distinguish meanings

Source: [One-to-One and Recursive Relationships — Modeling a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-d4aba820-39f-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 11. ch06-ai-d4aba820-39f-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Modeling a one-to-one relationship
- Concepts: ch06-relationship-labels, ch06-one-to-one
- Kind/difficulty: concept, 2/5
- Automated audit: **needs-edit** — chapter-fit, poor-explanation

Audit rationale: The item introduces a recursive Account-to-Account relationship but tags concepts for one-to-one and relationship labels from the chapter's one-to-one section. The explanation says 'self-related one-to-one relationship,' but the stem never states whether each account can back up only one other account or can have only one backup, so the one-to-one aspect is unsupported. As written, the pedagogically relevant idea is labeling an ambiguous recursive relationship, not one-to-one cardinality.

Suggested fix: Either remove ch06-one-to-one from the concept linkage and revise the explanation to focus only on labeling a recursive relationship, or explicitly add business rules showing the relationship is one-to-one on both sides.

### Prompt

A self-service portal links an Account to another Account when one account acts as a backup for the other. Why would adding a label such as 'backup for' or 'backs up' be helpful in the diagram?

| Option | Text |
| --- | --- |
| A ✓ | It makes the direction and meaning of the one-to-one link clear |
| B | It changes the relationship into many-to-many |
| C | It removes the need to decide which table holds the foreign key |
| D | It guarantees every account must participate |

Correct answer: **A — It makes the direction and meaning of the one-to-one link clear**

Explanation: A label clarifies what the link means, especially when the same entity participates on both ends of a self-related one-to-one relationship.

Review concept: Labeling ambiguous one-to-one relationships

Source: [One-to-One and Recursive Relationships — Modeling a one-to-one relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-one-to-one-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-d4aba820-39f-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 12. ch06-ai-82c308cd-4fe-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a recursive 1:m relationship
- Concepts: ch06-self-join
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: The stem, options, and keyed answer align cleanly. Exactly one option is defensible. SQL is correct for MySQL, uses appropriate aliases, and LEFT JOIN satisfies the 'if any' requirement. Distractors are plausible and the explanation teaches self-join logic well. Difficulty 2 is credible.

### Prompt

A company stores employees in a single table:

Employee(emp_id, full_name, manager_id)

where manager_id contains the emp_id of that employee's direct manager, or NULL for top-level managers.

Which query returns each employee's name together with their manager's name, if any?

Assume MySQL syntax and that the table has multiple employees.

| Option | Text |
| --- | --- |
| A ✓ | SELECT e.full_name, m.full_name AS manager_name<br>FROM Employee e<br>LEFT JOIN Employee m ON e.manager_id = m.emp_id; |
| B | SELECT e.full_name, m.full_name AS manager_name<br>FROM Employee e<br>JOIN Employee m ON e.emp_id = m.manager_id; |
| C | SELECT full_name, manager_id AS manager_name<br>FROM Employee; |
| D | SELECT e.full_name, m.full_name AS manager_name<br>FROM Employee e<br>LEFT JOIN Employee m ON e.emp_id = m.emp_id; |

Correct answer: **A — SELECT e.full_name, m.full_name AS manager_name
FROM Employee e
LEFT JOIN Employee m ON e.manager_id = m.emp_id;**

Explanation: Option A is correct because it uses a self join, treating Employee once as the employee row and once as the manager row. The join condition matches e.manager_id to m.emp_id, which finds the manager's row. LEFT JOIN is the right choice because the requirement says 'if any,' so employees with NULL manager_id must still appear, with manager_name returned as NULL. Option B reverses the relationship and finds subordinates instead of each employee's manager. Option C returns the manager's id, not the manager's name. Option D joins each row to itself by emp_id, so it would show the employee's own name as the manager name.

Review concept: Aliases let a query treat one table as separate logical roles so a row can be matched to a related row in the same table.

Source: [One-to-One and Recursive Relationships — Querying a recursive 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#querying-a-recursive-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-82c308cd-4fe-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 13. ch06-ai-82c308cd-4fe-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a recursive 1:m relationship
- Concepts: ch06-self-join
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Clear concept question with one unambiguous correct answer. Distractors are incorrect but plausible enough for a basic item. Explanation correctly teaches why aliases matter in self-joins. Difficulty 2 is appropriate.

### Prompt

Why are table aliases especially useful when querying a recursive relationship stored in a single table?

| Option | Text |
| --- | --- |
| A | They let the database enforce foreign keys automatically. |
| B ✓ | They let one table be referenced in more than one logical role within the same query. |
| C | They replace the need for a primary key. |
| D | They make recursive data unnecessary to store. |

Correct answer: **B — They let one table be referenced in more than one logical role within the same query.**

Explanation: When a row must be compared with another row in the same table, aliases give each role a separate name in the query. That makes the self join readable and unambiguous. Aliases do not enforce constraints, replace keys, or eliminate the need to store the recursive relationship.

Review concept: Aliases let a query treat one table as separate logical roles so a row can be matched to a related row in the same table.

Source: [One-to-One and Recursive Relationships — Querying a recursive 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#querying-a-recursive-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-82c308cd-4fe-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 14. ch06-ai-82c308cd-4fe-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a recursive 1:m relationship
- Concepts: ch06-recursive-query
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **needs-edit** — missing-assumption, poor-explanation

Audit rationale: The keyed SQL is correct in MySQL 8+ and matches the prompt, but the stem says only 'MySQL query' without stating a version or support for recursive CTEs. Since recursive CTE support is version-dependent, that assumption should be explicit. The explanation is accurate but could better note that this requires MySQL 8.0+ or equivalent support.

Suggested fix: Revise the stem to say 'Assume MySQL 8.0+.' In the explanation, add that recursive CTE support is required and that the anchor member includes dept_id 10 while the recursive member repeatedly finds children of already found departments.

### Prompt

A table Department(dept_id, dept_name, parent_dept_id) stores a hierarchy of departments. parent_dept_id is NULL for the top department and otherwise points to another Department row.

Which MySQL query returns every department at any depth under the department with dept_id = 10, including dept_id 10 itself?

| Option | Text |
| --- | --- |
| A ✓ | WITH RECURSIVE dept_tree AS (<br>  SELECT dept_id, dept_name, parent_dept_id<br>  FROM Department<br>  WHERE dept_id = 10<br>  UNION ALL<br>  SELECT d.dept_id, d.dept_name, d.parent_dept_id<br>  FROM Department d<br>  JOIN dept_tree t ON d.parent_dept_id = t.dept_id<br>)<br>SELECT * FROM dept_tree; |
| B | SELECT d.dept_id, d.dept_name, d.parent_dept_id<br>FROM Department d<br>JOIN Department p ON d.dept_id = p.parent_dept_id<br>WHERE p.dept_id = 10; |
| C | SELECT dept_id, dept_name, parent_dept_id<br>FROM Department<br>WHERE parent_dept_id = 10; |
| D | SELECT dept_id, dept_name, parent_dept_id<br>FROM Department<br>WHERE dept_id = 10 OR parent_dept_id IS NOT NULL; |

Correct answer: **A — WITH RECURSIVE dept_tree AS (
  SELECT dept_id, dept_name, parent_dept_id
  FROM Department
  WHERE dept_id = 10
  UNION ALL
  SELECT d.dept_id, d.dept_name, d.parent_dept_id
  FROM Department d
  JOIN dept_tree t ON d.parent_dept_id = t.dept_id
)
SELECT * FROM dept_tree;**

Explanation: A recursive common table expression starts with the anchor row (dept_id 10) and then repeatedly adds rows whose parent points to a row already found, which is how you traverse all descendant levels. B only finds one level and is not recursive. C returns only direct children. D does not follow the hierarchy and includes unrelated rows.

Review concept: Recursive structures can be traversed by repeatedly following self-references, using suitable common table expressions where supported.

Source: [One-to-One and Recursive Relationships — Querying a recursive 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#querying-a-recursive-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-82c308cd-4fe-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 15. ch06-ai-82c308cd-4fe-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a recursive 1:m relationship
- Concepts: ch06-recursive-query
- Kind/difficulty: sql-reading, 4/5
- Automated audit: **pass**

Audit rationale: The query shown is valid for MySQL 8+ and the question asks about the purpose of the recursive member, which makes option A uniquely correct. Distractors are plausible and clearly distinct. Explanation is conceptually solid. Difficulty 4 is acceptable because it asks about recursive CTE behavior, not just syntax recall.

### Prompt

Consider this MySQL query against a table Category(category_id, category_name, parent_category_id):

WITH RECURSIVE cte AS (
  SELECT category_id, category_name, parent_category_id
  FROM Category
  WHERE category_id = 7
  UNION ALL
  SELECT c.category_id, c.category_name, c.parent_category_id
  FROM Category c
  JOIN cte ON c.parent_category_id = cte.category_id
)
SELECT category_name
FROM cte;

What is the main purpose of the recursive part of this query?

| Option | Text |
| --- | --- |
| A ✓ | To repeatedly add rows whose parent points to a row already in the result so the hierarchy can be expanded beyond one level |
| B | To remove duplicate category names from the table before returning rows |
| C | To sort categories so that parents appear before children |
| D | To update each child row so that its parent_category_id becomes 7 |

Correct answer: **A — To repeatedly add rows whose parent points to a row already in the result so the hierarchy can be expanded beyond one level**

Explanation: The recursive SELECT joins the base result to Category again, using the self-reference to keep adding deeper descendants. That is what allows the query to walk through multiple levels of the hierarchy. It is not performing deduplication, sorting, or data modification.

Review concept: Recursive structures can be traversed by repeatedly following self-references, using suitable common table expressions where supported.

Source: [One-to-One and Recursive Relationships — Querying a recursive 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#querying-a-recursive-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-82c308cd-4fe-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 16. ch06-ai-f61cb28c-347-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Querying a recursive 1:m relationship
- Concepts: ch06-self-join
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: This is a sound self-join reading item. SQL in option A is correct, distractors are plausible, and the explanation clearly teaches aliases plus LEFT JOIN behavior for optional managers. Difficulty 3 is credible.

### Prompt

Assume MySQL and this table: employee(emp_id, emp_name, manager_id). manager_id is a self-referencing foreign key to employee.emp_id. Which query returns each employee together with their manager's name, if any?

| Option | Text |
| --- | --- |
| A ✓ | SELECT e.emp_name, m.emp_name AS manager_name FROM employee e LEFT JOIN employee m ON e.manager_id = m.emp_id; |
| B | SELECT e.emp_name, m.emp_name AS manager_name FROM employee e JOIN manager m ON e.emp_id = m.manager_id; |
| C | SELECT emp_name, manager_id FROM employee WHERE manager_id IS NOT NULL; |
| D | SELECT e.emp_name, m.emp_name AS manager_name FROM employee e LEFT JOIN employee m ON e.emp_id = m.emp_id; |

Correct answer: **A — SELECT e.emp_name, m.emp_name AS manager_name FROM employee e LEFT JOIN employee m ON e.manager_id = m.emp_id;**

Explanation: Option A is correct because the query must join employee to itself using aliases, one alias for the employee row and one for the manager row. The condition e.manager_id = m.emp_id matches each employee to the manager row, and LEFT JOIN keeps employees whose manager_id is NULL. Option B uses an undefined table and the wrong relationship direction. Option C returns the manager id rather than the manager's name. Option D joins each employee row to itself instead of to that employee's manager.

Review concept: Self joins and aliases

Source: [One-to-One and Recursive Relationships — Querying a recursive 1:m relationship](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#querying-a-recursive-1m-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-f61cb28c-347-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 17. ch06-ai-b690682f-bdc-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Recursive one-to-one and many-to-many relationships
- Concepts: ch06-recursive-cardinality
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The cardinality reasoning is correct and clearly framed around maximum participation. Exactly one answer is defensible, distractors are appropriate, and the explanation is accurate. Difficulty 2 fits.

### Prompt

An Employee entity has a recursive relationship named mentors. Business rules state: an employee may have at most one mentor, and an employee may mentor at most one other employee. Considering only the maximum participation on each side, what cardinality describes this recursive relationship?

| Option | Text |
| --- | --- |
| A ✓ | One-to-one |
| B | One-to-many |
| C | Many-to-many |
| D | Many-to-one |

Correct answer: **A — One-to-one**

Explanation: The correct answer is one-to-one because the maximum participation is 1 on both sides of the recursive relationship: each employee can have at most one mentor, and each employee can mentor at most one other employee. Participation can still be optional, but the maxima are still 1 and 1, which makes the relationship recursive one-to-one.

Review concept: Recursive cardinalities

Source: [One-to-One and Recursive Relationships — Recursive one-to-one and many-to-many relationships](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-recursive-many-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-b690682f-bdc-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 18. ch06-ai-b690682f-bdc-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Recursive one-to-one and many-to-many relationships
- Concepts: ch06-recursive-cardinality
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **needs-edit** — poor-explanation, missing-assumption

Audit rationale: Option A is the best match to the stated rule, but the item mixes conceptual cardinality with an implementation pattern that implies reciprocity and exclusivity without discussing enforcement. In a single partner_id column, reciprocity and one-to-one exclusivity are business rules that are not guaranteed by the column alone. The explanation should distinguish the rule from what the data pattern merely describes.

Suggested fix: Keep option A, but revise the stem or explanation to say 'best conceptual data pattern' rather than implying full enforcement by the column design. Add a note that reciprocal pairing and exclusivity are business rules that would require additional constraints or logic beyond just having partner_id.

### Prompt

A table named `Person` has columns `person_id` and `partner_id`. The business rule is: a person may have no partner or one partner; if two people are partners, each row points to the other person's `person_id`; and no person can be partnered with more than one other person. Which data pattern best matches this rule?

| Option | Text |
| --- | --- |
| A ✓ | A row may reference zero or one other row; if it references another row, that row references it back; and no row is partnered with more than one other row |
| B | Each row may reference many different rows, but each referenced row must be unique |
| C | Each row may reference at most one other row, and that row may be referenced by many rows |
| D | Each row must reference itself, and no other row is allowed |

Correct answer: **A — A row may reference zero or one other row; if it references another row, that row references it back; and no row is partnered with more than one other row**

Explanation: Option A matches a recursive one-to-one relationship with optional participation. A person can have zero or one partner, and any actual partnership must be reciprocal, with each row pointing back to the other. The rule also requires exclusivity, so one person cannot be the partner of several others. Option B describes many references. Option C allows many rows to point to the same partner, which breaks one-to-one cardinality. Option D describes self-reference, not partnering with another person.

Review concept: Recursive cardinalities

Source: [One-to-One and Recursive Relationships — Recursive one-to-one and many-to-many relationships](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-recursive-many-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-b690682f-bdc-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 19. ch06-ai-b690682f-bdc-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Recursive one-to-one and many-to-many relationships
- Concepts: ch06-recursive-cardinality
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: Straightforward and correct recursive cardinality question. Exactly one answer is defensible, distractors are plausible, and the explanation is sufficient for this level. Difficulty 3 is slightly generous but still acceptable.

### Prompt

A project may collaborate with several other projects, and each project may be collaborated with by several other projects. What cardinality should be used for this relationship among project rows?

| Option | Text |
| --- | --- |
| A | One-to-one |
| B | One-to-many |
| C ✓ | Many-to-many |
| D | Zero-to-one |

Correct answer: **C — Many-to-many**

Explanation: The relationship is many-to-many because one project can be related to multiple other projects, and each of those projects can also be related to multiple projects. That pattern is not one-to-one or one-to-many.

Review concept: Recursive cardinalities

Source: [One-to-One and Recursive Relationships — Recursive one-to-one and many-to-many relationships](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-recursive-many-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-b690682f-bdc-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 20. ch06-ai-b690682f-bdc-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Recursive one-to-one and many-to-many relationships
- Concepts: ch06-recursive-cardinality
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The prompt clearly specifies the parent-to-child perspective, preventing ambiguity between one-to-many and many-to-one. The keyed answer is correct, distractors are serviceable, and the explanation usefully notes the perspective issue. Difficulty 2 is credible.

### Prompt

A table `Category(category_id, parent_category_id)` stores categories where `parent_category_id` references `Category.category_id`. From the parent-category perspective, if one parent category can have several child categories and each child category has one parent, what recursive cardinality is being modeled?

| Option | Text |
| --- | --- |
| A | One-to-one |
| B ✓ | One-to-many |
| C | Many-to-many |
| D | Zero-to-zero |

Correct answer: **B — One-to-many**

Explanation: From the parent side, one category can be related to many child categories, while each child points back to one parent. That is a recursive one-to-many relationship. The parent/child perspective matters here: the same structure can be described as many-to-one from child to parent, but the question explicitly asks from parent to child.

Review concept: Recursive cardinalities

Source: [One-to-One and Recursive Relationships — Recursive one-to-one and many-to-many relationships](https://www.richardtwatson.com/open/Reader/_book/206-recursive.html#modeling-a-recursive-many-to-many-relationship)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch06-ai-b690682f-bdc-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

