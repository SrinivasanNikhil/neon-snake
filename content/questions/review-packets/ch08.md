# Chapter 8 question review packet

Candidates: 20. Automated semantic audit: gpt-5.4 at 2026-08-13T16:21:44.587Z.

> The automated audit is advisory. A person must verify the source, SQL behavior, answer uniqueness, wording, and explanation before approval.

## Chapter checklist

- [ ] Every question was read in full.
- [ ] Every SQL result or constraint claim was independently checked.
- [ ] Every correct answer is uniquely defensible.
- [ ] Every source section supports the tested concept.
- [ ] Wording is original, accessible, and not copied from the book.
- [ ] The chapter has acceptable section and difficulty coverage.

## 1. ch08-ai-4d44f4f0-752-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Boyce-Codd and higher normal forms
- Concepts: ch08-bcnf
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The functional dependencies and candidate keys are stated, and the keyed answer accurately captures the BCNF-specific issue: Advisor is a determinant but not a candidate key. The explanation correctly notes why 3NF can still hold when the dependent attribute is prime. Difficulty 2 is fair for a direct concept check with assumptions supplied.

### Prompt

Consider a relation R(Student, Course, Advisor) with functional dependencies {Student, Course} -> Advisor and Advisor -> Course. Assume the candidate keys are (Student, Course) and (Student, Advisor). Which issue shows that R can be in third normal form but still violate BCNF?

| Option | Text |
| --- | --- |
| A ✓ | A determinant is not a candidate key. |
| B | A nonkey attribute depends transitively on the key. |
| C | A nonkey attribute depends on only part of a composite key. |
| D | The relation contains multivalued attributes. |

Correct answer: **A — A determinant is not a candidate key.**

Explanation: BCNF requires every determinant to be a candidate key. In this relation, Advisor -> Course holds, but Advisor alone is not a candidate key, so BCNF is violated. The relation can still satisfy 3NF because Course is a prime attribute: it is part of the candidate key (Student, Course). That makes option A the best description of the BCNF-specific problem.

Review concept: BCNF identifies cases where a nonkey determinant remains in the relation.

Source: [Normalization and other Data Modeling Methods — Boyce-Codd and higher normal forms](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#boyce-codd-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-4d44f4f0-752-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 2. ch08-ai-4d44f4f0-752-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Boyce-Codd and higher normal forms
- Concepts: ch08-higher-normal-forms
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: This is a sound higher-normal-forms question. The scenario clearly describes independent sets of values for the same determinant, which is the hallmark of a multivalued dependency. Exactly one answer is defensible. Explanation is accurate and chapter-fit. Difficulty 3 is credible.

### Prompt

A table stores product, store, and approved_vendor. For each product, the approved vendors are independent of the store: a product may be sold in many stores and may have several approved vendors, and every allowed product-vendor pair can occur with every store. If the design still keeps all three facts in one relation, which kind of dependency problem is being modeled?

| Option | Text |
| --- | --- |
| A ✓ | A multivalued dependency |
| B | A partial dependency |
| C | A transitive dependency |
| D | A key dependency caused by a composite primary key |

Correct answer: **A — A multivalued dependency**

Explanation: When one attribute set determines multiple independent sets of values, and those sets combine freely with other attributes, the issue is a multivalued dependency. Higher normal forms are meant to address that kind of redundancy, which is beyond the first three normal forms.

Review concept: Higher normal forms handle dependency types such as multivalued dependencies.

Source: [Normalization and other Data Modeling Methods — Boyce-Codd and higher normal forms](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#boyce-codd-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-4d44f4f0-752-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 3. ch08-ai-4d44f4f0-752-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Boyce-Codd and higher normal forms
- Concepts: ch08-bcnf
- Kind/difficulty: concept, 4/5
- Automated audit: **pass**

Audit rationale: The stated FDs and candidate keys support the conclusion that C is a determinant but not a candidate key, so BCNF is violated. The explanation is factually correct and appropriately distinguishes BCNF from 3NF. Distractors are plausible enough. Difficulty 4 is acceptable.

### Prompt

A relation R(A, B, C) has functional dependencies AB -> C and C -> B. Assume the candidate keys are AB and AC. Which statement best explains why R violates Boyce-Codd normal form?

| Option | Text |
| --- | --- |
| A ✓ | It violates BCNF because C determines B, but C is not a candidate key. |
| B | It violates BCNF because AB is a composite key. |
| C | It violates BCNF because every third-normal-form relation automatically violates BCNF. |
| D | It violates BCNF because B is multivalued. |

Correct answer: **A — It violates BCNF because C determines B, but C is not a candidate key.**

Explanation: BCNF requires every determinant to be a candidate key. Here C -> B holds, but C alone is not a candidate key, so the relation violates BCNF. The setup is still compatible with 3NF because B is a prime attribute, appearing in candidate key AB. That is why A is correct and the others are not.

Review concept: BCNF can require further decomposition after 3NF when a nonkey determinant remains.

Source: [Normalization and other Data Modeling Methods — Boyce-Codd and higher normal forms](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#boyce-codd-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-4d44f4f0-752-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 4. ch08-ai-ccaac0af-4bb-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: First normal form
- Concepts: ch08-first-normal-form
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Correct 1NF item. A multivalued phone_numbers column violates single-valued attributes, and separate related rows are the best fix. Explanation is accurate and aligned with the source. Difficulty 2 is appropriate.

### Prompt

A company stores one row per customer order. In the column `phone_numbers`, some rows contain one number, while others contain several numbers separated by commas. Which change best brings the table into first normal form?

| Option | Text |
| --- | --- |
| A | Keep the column and add a rule that entries must use commas consistently |
| B ✓ | Split each phone number into a separate related row |
| C | Store the phone numbers in a longer text field |
| D | Move the customer name into a separate table |

Correct answer: **B — Split each phone number into a separate related row**

Explanation: First normal form requires each column value to be single-valued. When a fact can occur multiple times, it should be represented in related rows rather than as a repeated list inside one column.

Review concept: Single-valued attributes and repeating groups belong in related rows, not in one field.

Source: [Normalization and other Data Modeling Methods — First normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#first-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-ccaac0af-4bb-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 5. ch08-ai-ccaac0af-4bb-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: First normal form
- Concepts: ch08-first-normal-form
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The table design in option B is the only one in 1NF under the stated assumption of one row per invoice line. The distractors represent common anti-patterns: lists in a cell and repeating columns. Explanation is clear. Difficulty 2 is suitable.

### Prompt

Which table design is in first normal form? Assume one row represents one invoice line.

| Option | Text |
| --- | --- |
| A | An invoice table with a `product_ids` column that stores `P12;P15;P19` in one cell |
| B ✓ | An invoice table with one row per line item and one `product_id` value in each row |
| C | An invoice table with separate columns `product1`, `product2`, and `product3` |
| D | An invoice table with a `products` column that stores both product codes and quantities as a semicolon-separated list |

Correct answer: **B — An invoice table with one row per line item and one `product_id` value in each row**

Explanation: Option B is correct because first normal form requires single-valued columns, so each row should store one product_id for one invoice line. Options A and D store multiple values in one column, and option C uses repeating columns for the same kind of fact. Those designs violate the chapter's idea that repeating or multivalued facts should be represented in related rows instead.

Review concept: 1NF requires single-valued attributes and avoids lists or repeating groups inside a row.

Source: [Normalization and other Data Modeling Methods — First normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#first-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-ccaac0af-4bb-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 6. ch08-ai-ccaac0af-4bb-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: First normal form
- Concepts: ch08-first-normal-form
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: Solid 1NF repair question. Creating a separate SupplierEmail table is the standard fix for a multivalued attribute. Distractors are plausible and incorrect. Explanation is accurate. Difficulty 3 is credible.

### Prompt

A `Supplier` table has columns `supplier_id`, `name`, and `contact_emails`. The `contact_emails` column sometimes contains multiple email addresses in one cell. Which design is the best first-normal-form fix? Assume you still need to keep all email addresses for each supplier.

| Option | Text |
| --- | --- |
| A | Leave the table as is and require semicolons between email addresses |
| B ✓ | Create a separate `SupplierEmail` table with one row per email address and a supplier reference |
| C | Add more columns such as `email2` and `email3` to the `Supplier` table |
| D | Store only the first email address and discard the rest |

Correct answer: **B — Create a separate `SupplierEmail` table with one row per email address and a supplier reference**

Explanation: First normal form is violated when a column contains multiple values for the same fact. The usual fix is to move the repeating fact into a separate related table so each row contains a single email address.

Review concept: Use related rows for repeating facts so each column remains single-valued.

Source: [Normalization and other Data Modeling Methods — First normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#first-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-ccaac0af-4bb-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 7. ch08-ai-ccaac0af-4bb-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: First normal form
- Concepts: ch08-first-normal-form
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: This correctly identifies the main 1NF issue as a non-single-valued attribute. Option D is a plausible distractor because it overstates the remedy, which helps discriminate understanding. Explanation is accurate. Difficulty 3 is reasonable.

### Prompt

A payroll table has one row per employee. The `skills` attribute contains values like `Excel;SQL;Python` for some employees and a single value for others. What is the main normalization problem here?

| Option | Text |
| --- | --- |
| A | The table has too many rows for each employee |
| B ✓ | The `skills` attribute is not single-valued |
| C | The employee name should be the primary key |
| D | The table must be split into one table per skill |

Correct answer: **B — The `skills` attribute is not single-valued**

Explanation: The issue is that one column holds multiple skill values for some employees. First normal form requires each attribute value to be single and atomic; repeating facts should be stored in related rows.

Review concept: Detect multivalued attributes as a first-normal-form violation.

Source: [Normalization and other Data Modeling Methods — First normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#first-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-ccaac0af-4bb-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 8. ch08-ai-cc519a70-6c2-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Normalization
- Concepts: ch08-functional-dependency
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Simple and correct functional-dependency item. The mapping described supports only employee number -> employee name. Explanation is accurate. Difficulty 1 is appropriate.

### Prompt

In a company database, each employee number maps to exactly one employee name, while the same name can appear for more than one employee number. What does the dependency between employee number and employee name mean?

| Option | Text |
| --- | --- |
| A ✓ | Employee number functionally determines employee name |
| B | Employee name functionally determines employee number |
| C | Employee number and employee name determine each other in both directions |
| D | Neither attribute determines the other |

Correct answer: **A — Employee number functionally determines employee name**

Explanation: If one attribute set always determines the value of another, that is a functional dependency. Here, a given employee number identifies one employee name, even if names are not unique.

Review concept: Recognize when one attribute set determines another.

Source: [Normalization and other Data Modeling Methods — Normalization](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#normalization)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-cc519a70-6c2-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 9. ch08-ai-cc519a70-6c2-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Normalization
- Concepts: ch08-anomalies
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The scenario cleanly describes repeated customer facts causing update inconsistency, which is a modification anomaly. Distractors are plausible enough and only one answer is defensible. Explanation is correct. Difficulty 2 fits.

### Prompt

A sales table stores customer email, customer phone, and order data in the same row. When a customer changes phone number, every past order row must be updated or the database becomes inconsistent. What kind of problem is this?

| Option | Text |
| --- | --- |
| A ✓ | A modification anomaly |
| B | A missing foreign key |
| C | A transaction isolation issue |
| D | A view maintenance issue |

Correct answer: **A — A modification anomaly**

Explanation: When the same fact is repeated across multiple rows, updating it can require many changes and create inconsistent values if some are missed. That is a modification anomaly.

Review concept: Identify insert, update, and delete anomalies caused by poor structure.

Source: [Normalization and other Data Modeling Methods — Normalization](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#normalization)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-cc519a70-6c2-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 10. ch08-ai-cc519a70-6c2-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Normalization
- Concepts: ch08-functional-dependency
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: The business rule directly supports customer_id -> customer_name, making the answer unambiguous. The explanation properly connects the FD to normalization analysis. Difficulty 3 is slightly generous but still acceptable.

### Prompt

A table Orders(order_id, customer_id, customer_name, order_date) has the rule that each customer_id corresponds to exactly one customer_name. Assume order_id is the primary key. Which nonkey functional dependency is most useful for normalization analysis?

| Option | Text |
| --- | --- |
| A ✓ | customer_id -> customer_name |
| B | customer_name -> customer_id |
| C | order_date -> customer_id |
| D | customer_name -> order_date |

Correct answer: **A — customer_id -> customer_name**

Explanation: The stated business rule says customer_id determines customer_name, so customer_id -> customer_name is the relevant nonkey functional dependency. It is especially useful in normalization because it reveals that customer_name may be repeated across many orders due to a dependency other than the primary key. The other options are not supported by the stated rule.

Review concept: Use a stated business rule to identify a functional dependency.

Source: [Normalization and other Data Modeling Methods — Normalization](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#normalization)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-cc519a70-6c2-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 11. ch08-ai-cc519a70-6c2-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Normalization
- Concepts: ch08-anomalies
- Kind/difficulty: concept, 4/5
- Automated audit: **needs-edit** — ambiguous-answer, poor-explanation

Audit rationale: The scenario specifically illustrates insertion and deletion anomalies, but the keyed answer and explanation collapse these under the broader phrase 'modification anomalies.' Some texts use modification anomaly broadly, but many learners will distinguish insert, update, and delete anomalies separately. Because the stem asks what it 'most clearly illustrates,' the wording can cause ambiguity between the umbrella category and the specific anomaly types mentioned in the stem.

Suggested fix: Revise the stem to ask for the broader structural category, or add options that explicitly distinguish insertion/deletion anomalies from the umbrella term. For example: 'What broader structural problem do these insertion and deletion problems illustrate?' Then keep the current answer.

### Prompt

A single relation mixes book facts, author facts, and borrowing facts in the same rows, using attributes such as book_title, author_name, and borrower_contact. If a book has never been borrowed, storing its author information is awkward. If the last borrowing row for a book is deleted, the only stored author information for that book may also disappear. What structural problem does this most clearly illustrate?

| Option | Text |
| --- | --- |
| A ✓ | Modification anomalies from storing unrelated facts together |
| B | A need for more indexes on author_name |
| C | A requirement to use a composite primary key |
| D | A referential integrity failure caused by cascade rules |

Correct answer: **A — Modification anomalies from storing unrelated facts together**

Explanation: This design is vulnerable to modification anomalies because one relation combines facts that should be stored separately. The difficulty of storing author information before any borrowing occurs is an insertion anomaly, and losing author information when the last borrowing row is deleted is a deletion anomaly. Those are classic modification anomalies caused by poor structure.

Review concept: Connect redundant storage to insert, update, and delete anomalies.

Source: [Normalization and other Data Modeling Methods — Normalization](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#normalization)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-cc519a70-6c2-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 12. ch08-ai-7bd08f64-c03-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Other data modeling methods
- Concepts: ch08-method-equivalence
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Accurate chapter-fit question on equivalence of design methods. Only option B reflects the source idea that different methods should converge on structures representing the same facts. Explanation is concise but adequate. Difficulty 2 is appropriate.

### Prompt

A team designs a customer-order database using two approaches: one person starts from a conceptual model, while another starts by checking dependencies and normal forms. If both are done well, what outcome should you expect?

| Option | Text |
| --- | --- |
| A | They should usually produce completely different tables because the methods optimize for different facts. |
| B ✓ | They should converge on structures that represent the same real-world facts, even if the design process differs. |
| C | The normalization-based design must always contain more tables than the conceptual-model-based design. |
| D | The conceptual model must always be recreated inside a single wide table for reporting. |

Correct answer: **B — They should converge on structures that represent the same real-world facts, even if the design process differs.**

Explanation: The two methods use different procedures, but the goal is the same: a structure that accurately represents the required facts. If both are applied correctly, the resulting design should be equivalent in what it models, even if the steps taken to reach it differ.

Review concept: Alternative design methods

Source: [Normalization and other Data Modeling Methods — Other data modeling methods](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#other-data-modeling-methods)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-7bd08f64-c03-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 13. ch08-ai-7bd08f64-c03-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Other data modeling methods
- Concepts: ch08-method-equivalence
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The stem states the key assumption that both designs were created correctly, which makes option A uniquely defensible. The distractors are clearly wrong but still plausible misconceptions. Explanation is accurate and relevant. Difficulty 3 is acceptable.

### Prompt

A retailer is comparing two schema designs for the same business rules. Design X was built from a dependency analysis; Design Y was built from a conceptual data model. Assume both were created correctly. Which statement best describes their relationship?

| Option | Text |
| --- | --- |
| A ✓ | They should model the same facts, though they may use different structures and design steps. |
| B | Design X must be in BCNF, while Design Y cannot be normalized at all. |
| C | Design Y must always store repeated values, while Design X must never do so. |
| D | They can only be equivalent if they use identical table names and column names. |

Correct answer: **A — They should model the same facts, though they may use different structures and design steps.**

Explanation: Correct design methods can differ in procedure and still lead to equivalent data structures in terms of the facts they capture. Table and column names do not have to match for the designs to represent the same business reality.

Review concept: Alternative design methods

Source: [Normalization and other Data Modeling Methods — Other data modeling methods](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#other-data-modeling-methods)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-7bd08f64-c03-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 14. ch08-ai-7bd08f64-c03-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Other data modeling methods
- Concepts: ch08-method-equivalence
- Kind/difficulty: concept, 4/5
- Automated audit: **needs-edit** — near-duplicate, misrated-difficulty

Audit rationale: This item tests essentially the same concept and same inference as the previous two method-equivalence questions, with only superficial wording changes. It adds little new educational value and is not substantively harder despite being rated 4. The keyed answer is correct, but the set is redundant.

Suggested fix: Replace with a distinct application question, such as asking how to evaluate whether two differently named schemas capture the same facts or constraints. Lower the difficulty if kept in its current form.

### Prompt

Two analysts independently design a database for the same company. One uses normalization rules; the other uses a conceptual modeling method. They disagree on the process but both claim the final schema is correct. What is the strongest reason this can happen?

| Option | Text |
| --- | --- |
| A ✓ | Different design methods can still converge on a schema that captures the same underlying facts. |
| B | Only the conceptual method can produce a schema that avoids anomalies. |
| C | Normalization and conceptual modeling always produce identical SQL statements. |
| D | A correct schema must preserve every intermediate design decision made during analysis. |

Correct answer: **A — Different design methods can still converge on a schema that captures the same underlying facts.**

Explanation: The key idea is convergence: different methods can reach a valid representation of the same domain facts. The methods are not identical in procedure, but a correct result from either should be faithful to the business reality being modeled.

Review concept: Alternative design methods

Source: [Normalization and other Data Modeling Methods — Other data modeling methods](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#other-data-modeling-methods)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-7bd08f64-c03-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 15. ch08-ai-0cfda02d-6a3-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Second normal form
- Concepts: ch08-second-normal-form
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Stem, options, and keyed answer align. Exactly one answer is defensible: moving EmployeeName and ProjectName to entity tables removes partial dependencies on parts of the composite key, which is the core 2NF issue. Distractors are plausible but clearly incorrect. Explanation is accurate and instructional. Difficulty 2 is credible.

### Prompt

A company stores project work in a table with primary key (EmployeeID, ProjectID). The table also includes EmployeeName and ProjectName. Which change would best help move this table into second normal form?

| Option | Text |
| --- | --- |
| A | Keep one table, but add indexes on EmployeeID and ProjectID |
| B ✓ | Move EmployeeName to an Employee table and ProjectName to a Project table |
| C | Add a computed column that concatenates EmployeeID and ProjectID |
| D | Rename the primary key columns so they are more descriptive |

Correct answer: **B — Move EmployeeName to an Employee table and ProjectName to a Project table**

Explanation: Second normal form requires that no nonkey attribute depend on only part of a composite key. EmployeeName depends on EmployeeID alone, and ProjectName depends on ProjectID alone, so each should be separated into its own table.

Review concept: Second normal form

Source: [Normalization and other Data Modeling Methods — Second normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#second-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-0cfda02d-6a3-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 16. ch08-ai-0cfda02d-6a3-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Second normal form
- Concepts: ch08-second-normal-form
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: Clear assumptions are stated, and the question correctly targets partial dependency under a composite key. StudentName is the only nonkey attribute depending on only StudentID, so exactly one answer is correct. Explanation correctly distinguishes nonkey attributes from key attributes. Difficulty 3 is reasonable.

### Prompt

A table named Enrollment has this structure: Enrollment(StudentID, CourseID, StudentName, Grade), with primary key (StudentID, CourseID). Assume each StudentID identifies exactly one student name, and Grade depends on the student's enrollment in a specific course. Which attribute is a second-normal-form violation because it depends on only part of the composite key?

| Option | Text |
| --- | --- |
| A | Grade |
| B ✓ | StudentName |
| C | StudentID |
| D | CourseID |

Correct answer: **B — StudentName**

Explanation: Second normal form removes nonkey attributes that depend on only part of a composite key. Here the key is (StudentID, CourseID). StudentName depends only on StudentID, not on the full key, so it is a partial dependency and a 2NF violation. Grade depends on the full enrollment, while StudentID and CourseID are key attributes rather than nonkey attributes.

Review concept: Second normal form

Source: [Normalization and other Data Modeling Methods — Second normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#second-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-0cfda02d-6a3-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 17. ch08-ai-0cfda02d-6a3-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Second normal form
- Concepts: ch08-second-normal-form
- Kind/difficulty: concept, 4/5
- Automated audit: **pass**

Audit rationale: The stem provides enough assumptions to infer that OrderDate depends on OrderID and ProductName depends on ProductID, making both partial dependencies and thus 2NF violations. Only option C is correct. Distractors are plausible. Explanation teaches the concept well. Difficulty 4 is acceptable because the learner must evaluate multiple attributes.

### Prompt

A relation has a composite primary key (OrderID, ProductID) and the following nonkey attributes: OrderDate, ProductName, and Quantity. Assume each order can contain many products, and each product can appear in many orders. Which statement best describes the normalization issue?

| Option | Text |
| --- | --- |
| A | The table is already in second normal form because each row represents one order-product combination |
| B | Only ProductName violates second normal form because it depends on ProductID alone |
| C ✓ | OrderDate and ProductName violate second normal form because each depends on only part of the key |
| D | Quantity violates second normal form because it must depend on both key columns |

Correct answer: **C — OrderDate and ProductName violate second normal form because each depends on only part of the key**

Explanation: With a composite key, any nonkey attribute that depends on just one component is a partial dependency. OrderDate depends on OrderID alone, and ProductName depends on ProductID alone, so both break second normal form. Quantity depends on the full combination and does not create that specific problem.

Review concept: Second normal form

Source: [Normalization and other Data Modeling Methods — Second normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#second-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-0cfda02d-6a3-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 18. ch08-ai-7dbbcc25-692-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Third normal form
- Concepts: ch08-third-normal-form
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The scenario correctly describes a transitive dependency: OrderID identifies CustomerID, and CustomerID determines customer details. Option B is the best normalization step toward 3NF. Explanation is accurate and clear. Difficulty 2 is appropriate.

### Prompt

A retail database stores each order row with OrderID, CustomerID, CustomerName, and CustomerCity. CustomerName and CustomerCity are determined by CustomerID, not by OrderID. What design change best moves the structure toward third normal form?

| Option | Text |
| --- | --- |
| A | Keep all columns in one table because OrderID already identifies the row |
| B ✓ | Move CustomerName and CustomerCity to a separate Customer table keyed by CustomerID |
| C | Split OrderID into two columns so the customer data becomes atomic |
| D | Replace CustomerID with CustomerName as the primary key in the order table |

Correct answer: **B — Move CustomerName and CustomerCity to a separate Customer table keyed by CustomerID**

Explanation: Third normal form removes nonkey facts that depend on other nonkey facts instead of directly on the key. Here, customer details depend on CustomerID, so they belong in a separate Customer table rather than being repeated in every order row.

Review concept: Identify and eliminate transitive dependencies to reach third normal form.

Source: [Normalization and other Data Modeling Methods — Third normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#third-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-7dbbcc25-692-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 19. ch08-ai-7dbbcc25-692-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Third normal form
- Concepts: ch08-third-normal-form
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: Good 3NF scenario with explicit assumptions. ProductName and UnitPrice depend on ProductID rather than directly on the key, so separating Product is the correct redesign. Distractors are plausible and meaningfully distinct. Explanation is strong and teaches the transitive-dependency idea. Difficulty 3 is credible.

### Prompt

Assume InvoiceLine has a single-column primary key InvoiceLineID and columns (InvoiceLineID, InvoiceID, ProductID, ProductName, UnitPrice, Qty). Each row records one product on one invoice. ProductName and UnitPrice are determined by ProductID, not directly by InvoiceLineID. Which redesign best addresses the third-normal-form problem?

| Option | Text |
| --- | --- |
| A | Keep InvoiceLine(InvoiceLineID, InvoiceID, ProductID, ProductName, UnitPrice, Qty) unchanged so every invoice line stores product details |
| B ✓ | Create Product(ProductID, ProductName, UnitPrice) and keep InvoiceLine(InvoiceLineID, InvoiceID, ProductID, Qty) |
| C | Create Invoice(InvoiceID, ProductName, UnitPrice, Qty) and remove ProductID entirely |
| D | Move Qty into Product so Product(ProductID, ProductName, UnitPrice, Qty) and keep InvoiceLine(InvoiceLineID, InvoiceID, ProductID) |

Correct answer: **B — Create Product(ProductID, ProductName, UnitPrice) and keep InvoiceLine(InvoiceLineID, InvoiceID, ProductID, Qty)**

Explanation: Option B is correct because ProductName and UnitPrice depend on ProductID, which is a nonkey attribute in InvoiceLine. That means those product facts are transitively dependent on the key InvoiceLineID through ProductID, which violates third normal form. Separating Product(ProductID, ProductName, UnitPrice) leaves InvoiceLine with only facts about a specific invoice line: which invoice, which product, and what quantity. The other options either keep the transitive dependency, discard the identifier needed to relate products, or move Qty away from the invoice line where it belongs.

Review concept: In 3NF, nonkey attributes should depend directly on the key, not on another nonkey attribute such as ProductID.

Source: [Normalization and other Data Modeling Methods — Third normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#third-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-7dbbcc25-692-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 20. ch08-ai-7dbbcc25-692-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Third normal form
- Concepts: ch08-third-normal-form
- Kind/difficulty: concept, 4/5
- Automated audit: **pass**

Audit rationale: The stem clearly states the candidate-key assumption and the manager-sharing assumption needed to infer the transitive dependency. Option A is uniquely correct. The explanation accurately rules out 1NF and 2NF distractors. Difficulty 4 is somewhat high but still defensible because learners must reason through the dependency chain.

### Prompt

Assume DepartmentID is the only candidate key in Department(DepartmentID, DepartmentName, ManagerID, ManagerPhone), and a manager may manage multiple departments. ManagerPhone depends on ManagerID rather than directly on DepartmentID. Which statement best describes the normalization issue?

| Option | Text |
| --- | --- |
| A ✓ | The table violates third normal form because a nonkey attribute depends on another nonkey attribute |
| B | The table violates first normal form because ManagerPhone is a repeating group |
| C | The table violates second normal form because ManagerPhone depends on only part of a composite key |
| D | The table is already in third normal form because DepartmentID is a primary key |

Correct answer: **A — The table violates third normal form because a nonkey attribute depends on another nonkey attribute**

Explanation: Option A is correct because DepartmentID determines ManagerID, and ManagerID determines ManagerPhone. Since ManagerID is a nonkey attribute and ManagerPhone depends on it, ManagerPhone does not depend directly on the key. That is the classic 3NF problem of a nonkey attribute depending on another nonkey attribute. Option B is wrong because ManagerPhone is a single-valued column, not a repeating group. Option C is wrong because there is no composite key in the stated assumptions. Option D is wrong because having a primary key does not by itself guarantee 3NF.

Review concept: A 3NF violation occurs when a nonkey fact is determined by another nonkey fact instead of directly by the key.

Source: [Normalization and other Data Modeling Methods — Third normal form](https://www.richardtwatson.com/open/Reader/_book/208-normalization.html#third-normal-form)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch08-ai-7dbbcc25-692-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

