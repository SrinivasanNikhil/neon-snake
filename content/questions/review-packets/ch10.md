# Chapter 10 question review packet

Candidates: 21. Automated semantic audit: gpt-5.4 at 2026-08-13T16:22:22.844Z.

> The automated audit is advisory. A person must verify the source, SQL behavior, answer uniqueness, wording, and explanation before approval.

## Chapter checklist

- [ ] Every question was read in full.
- [ ] Every SQL result or constraint claim was independently checked.
- [ ] Every correct answer is uniquely defensible.
- [ ] Every source section supports the tested concept.
- [ ] Wording is original, accessible, and not copied from the book.
- [ ] The chapter has acceptable section and difficulty coverage.

## 1. ch10-ai-35a215cc-a05-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a table and data types
- Concepts: ch10-definition
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The item correctly targets CREATE TABLE as data definition. Option A is uniquely correct, distractors are plausible enough, and the explanation is accurate and instructive. Difficulty 2 fits the basic concept.

### Prompt

A company wants to create a table for tracking equipment loans. Which statement best describes what a CREATE TABLE statement does?

| Option | Text |
| --- | --- |
| A ✓ | It defines the table structure, including columns, data types, and constraints such as keys or nullability. |
| B | It inserts initial rows into the table and fills every column with default values. |
| C | It changes the storage engine of an existing table without altering its columns. |
| D | It creates a temporary result set that exists only for the current query. |

Correct answer: **A — It defines the table structure, including columns, data types, and constraints such as keys or nullability.**

Explanation: CREATE TABLE is used for data definition: it specifies the relation’s columns and the rules that apply to them, such as data types, keys, and whether values may be null. The other options describe data loading, storage changes, or query results, not table definition.

Review concept: Data definition

Source: [SQL — Creating a table and data types](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=216)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-35a215cc-a05-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 2. ch10-ai-35a215cc-a05-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a table and data types
- Concepts: ch10-data-types
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: The stem makes the intended assumption explicit through the use case of comparisons and age calculations. A temporal type is the only sound choice. Distractors are plausible and incorrect. Explanation is accurate and teaches data type matching. Difficulty 3 is fair.

### Prompt

A customer database stores a column called birth_date, and the application often needs to compare dates and calculate ages. Which data type choice is most appropriate?

| Option | Text |
| --- | --- |
| A | A character type, because dates are easiest to sort alphabetically. |
| B ✓ | A temporal type, because it matches date-based values and date operations. |
| C | A large-object type, because birthdays can vary in length. |
| D | A Boolean type, because a birth date is either present or absent. |

Correct answer: **B — A temporal type, because it matches date-based values and date operations.**

Explanation: A temporal type is the best fit when the column stores dates and supports date-related operations such as comparison or arithmetic. Character and Boolean types do not model the value domain as well, and a large-object type is far too broad for a date field.

Review concept: Choosing data types

Source: [SQL — Creating a table and data types](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=216)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-35a215cc-a05-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 3. ch10-ai-35a215cc-a05-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a table and data types
- Concepts: ch10-collation
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: This is a clean concept check on collation. Only option A governs locale-specific text ordering. Distractors are plausible and clearly distinct. Explanation is correct and concise. Difficulty 3 is acceptable.

### Prompt

An online store serves users in multiple languages. It needs product names to sort according to the rules of a specific locale. Which database feature controls that behavior?

| Option | Text |
| --- | --- |
| A ✓ | Collation |
| B | Normalization |
| C | Replication |
| D | Partitioning |

Correct answer: **A — Collation**

Explanation: Collation determines how character values are compared and ordered, including locale-specific sorting rules. The other choices affect storage, duplication, or data organization, but not text comparison and ordering rules.

Review concept: Collation

Source: [SQL — Creating a table and data types](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=216)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-35a215cc-a05-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 4. ch10-ai-35a215cc-a05-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a table and data types
- Concepts: ch10-data-types
- Kind/difficulty: concept, 4/5
- Automated audit: **pass**

Audit rationale: The stem properly states the key assumption that the values are identifiers, not quantities, which makes a character type clearly best. Exactly one answer is defensible, and the explanation addresses leading zeros and exact matching well. Difficulty 4 is a little high but still defensible because the item tests type-selection reasoning rather than recall alone.

### Prompt

A table column stores short status codes such as `A1`, `B2`, and `007`. The values are identifiers used for exact matching and indexing, not quantities for arithmetic. Which data type strategy is best?

| Option | Text |
| --- | --- |
| A ✓ | Use a character type sized to fit the codes. |
| B | Use a Boolean type because there are only a few possible statuses. |
| C | Use a large-object text type so future codes have no practical limit. |
| D | Use a temporal type because statuses can change over time. |

Correct answer: **A — Use a character type sized to fit the codes.**

Explanation: A character type is the best fit because the values are short text identifiers and may include letters or leading zeros, such as `007`. A Boolean type is too limited for multiple distinct codes, a large-object type is unnecessary for short values, and a temporal type stores dates or times rather than status codes.

Review concept: Choosing data types

Source: [SQL — Creating a table and data types](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=216)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-35a215cc-a05-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 5. ch10-ai-35a215cc-a05-5

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Creating a table and data types
- Concepts: ch10-collation
- Kind/difficulty: concept, 4/5
- Automated audit: **pass**

Audit rationale: The item correctly attributes differences in case-sensitive ordering/comparison behavior to collation. Only option A fits. Distractors are plausible and non-overlapping. Explanation is accurate and helpful. Difficulty 4 is slightly generous but still acceptable.

### Prompt

Two databases store the same customer names. In one database, uppercase and lowercase letters are treated as equivalent for ordering; in the other, they are not. What is the most likely reason for the difference?

| Option | Text |
| --- | --- |
| A ✓ | They use different collation rules for character comparison and sorting. |
| B | They use different primary keys for the customer table. |
| C | They use different transaction isolation levels. |
| D | They use different aggregate functions when reading the names. |

Correct answer: **A — They use different collation rules for character comparison and sorting.**

Explanation: Collation controls how text is compared and ordered, so it can determine whether case differences affect sorting or comparison. Keys, isolation levels, and aggregates do not define string ordering rules.

Review concept: Collation

Source: [SQL — Creating a table and data types](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=216)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-35a215cc-a05-5 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 6. ch10-ai-0212a664-dd9-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Privileges and application use
- Concepts: ch10-privileges
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The stem, options, and keyed answer align cleanly. Exactly one answer is defensible: granting only SELECT matches read-only access. Distractors are plausible and clearly incorrect for the stated goal. The explanation is accurate and instructional. Difficulty 2 is appropriate.

### Prompt

A database administrator wants the reporting role to read rows from the `invoices` table but not change or delete any invoice data. Which action best fits that goal?

| Option | Text |
| --- | --- |
| A ✓ | Use `GRANT SELECT ON invoices` for the reporting role |
| B | Use `GRANT UPDATE ON invoices` for the reporting role |
| C | Use `GRANT ALL PRIVILEGES ON invoices` for the reporting role |
| D | Use `REVOKE SELECT ON invoices` from the reporting role |

Correct answer: **A — Use `GRANT SELECT ON invoices` for the reporting role**

Explanation: `GRANT` assigns specific privileges on a database object. If the role should only read from `invoices`, granting only `SELECT` gives query access without also granting modification privileges such as `UPDATE` or `DELETE`. `GRANT UPDATE` would allow changes, `GRANT ALL PRIVILEGES` would allow more than read access, and `REVOKE SELECT` would remove read access instead of providing it.

Review concept: GRANT and REVOKE

Source: [SQL — Privileges and application use](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-0212a664-dd9-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 7. ch10-ai-0212a664-dd9-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Privileges and application use
- Concepts: ch10-privileges
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The SQL statement in option A is correct under the stated standard-SQL assumption, and exactly one option grants read-only table access. Option D is a plausible distractor because a view alone does not grant privileges. Explanation is correct and useful. Difficulty 3 is acceptable.

### Prompt

Assume standard SQL privilege syntax and that user names are already valid in the DBMS. An analyst should be able to read rows from the `customer` table but not modify them. Which statement best grants that capability?

| Option | Text |
| --- | --- |
| A ✓ | GRANT SELECT ON customer TO analyst; |
| B | REVOKE SELECT ON customer FROM analyst; |
| C | GRANT UPDATE ON customer TO analyst; |
| D | CREATE VIEW customer_public AS SELECT * FROM customer; |

Correct answer: **A — GRANT SELECT ON customer TO analyst;**

Explanation: `GRANT SELECT ON customer TO analyst;` gives the analyst read access to the table. `REVOKE` removes a privilege rather than granting one, and `GRANT UPDATE` would allow changes to data. Creating a view does not itself grant any privilege to the analyst; it only defines another database object.

Review concept: GRANT and REVOKE

Source: [SQL — Privileges and application use](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-0212a664-dd9-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 8. ch10-ai-0212a664-dd9-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Privileges and application use
- Concepts: ch10-embedded-sql
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The concept is accurately tested: embedded SQL places SQL inside a procedural program, unlike interactive SQL at a prompt. Only option B fits. Distractors are plausible without creating ambiguity. Explanation teaches the distinction clearly. Difficulty 2 is credible.

### Prompt

Which situation best describes embedded SQL rather than interactive SQL?

| Option | Text |
| --- | --- |
| A | A user types queries directly at a database prompt |
| B ✓ | SQL statements are written inside a procedural application that also contains program logic |
| C | A table is created with a primary key constraint |
| D | A report is produced from a grouped aggregate query |

Correct answer: **B — SQL statements are written inside a procedural application that also contains program logic**

Explanation: Embedded SQL means SQL is placed inside a program, while the surrounding procedural code handles control flow and application logic. Typing commands at a prompt is interactive SQL.

Review concept: Interactive and embedded SQL

Source: [SQL — Privileges and application use](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-0212a664-dd9-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 9. ch10-ai-0212a664-dd9-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Privileges and application use
- Concepts: ch10-embedded-sql
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: The scenario clearly describes procedural control flow around row retrieval and storage, matching embedded SQL usage. Only option A is defensible. Explanation is accurate and links the scenario to the concept well. Difficulty 3 is reasonable.

### Prompt

A payroll program runs this logic:

1. Retrieve the next employee row.
2. If the employee exists, compute tax in application code.
3. Store the result.
4. Repeat until no rows remain.

Which statement best characterizes this design?

| Option | Text |
| --- | --- |
| A ✓ | It relies on embedded SQL with row-by-row processing handled by the program |
| B | It is purely interactive SQL because each row is handled separately |
| C | It is a GRANT/REVOKE workflow because privileges control the loop |
| D | It is a view definition because the database manages all control flow |

Correct answer: **A — It relies on embedded SQL with row-by-row processing handled by the program**

Explanation: The SQL is being used inside a program that controls the loop and processes rows one at a time. That matches embedded SQL with procedural row-by-row application logic, not interactive SQL or a privilege or view operation.

Review concept: Interactive and embedded SQL

Source: [SQL — Privileges and application use](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-0212a664-dd9-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 10. ch10-ai-8f4bf742-de4-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Scalar and aggregate functions
- Concepts: ch10-scalar-functions
- Kind/difficulty: sql-scenario, 2/5
- Automated audit: **pass**

Audit rationale: The item accurately distinguishes scalar from aggregate functions using row-level transformations. Only option A is correct. Distractors are plausible, and the explanation clearly teaches the one-value-per-row idea. Difficulty 2 is appropriate.

### Prompt

A reporting query needs to display each employee's last name in uppercase and the length of their first name. Which SQL feature is being used when the query applies functions to each row’s column values one row at a time?

| Option | Text |
| --- | --- |
| A ✓ | Scalar functions |
| B | Aggregate functions |
| C | Indexes |
| D | Views |

Correct answer: **A — Scalar functions**

Explanation: Scalar functions operate on individual values from each row and return one result per evaluated row. Uppercasing a name and measuring its length are both row-by-row transformations, not summaries across multiple rows.

Review concept: Scalar functions transform one value per evaluated row, including text, numeric, and date-time values.

Source: [SQL — Scalar and aggregate functions](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=223)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-8f4bf742-de4-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 11. ch10-ai-8f4bf742-de4-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Scalar and aggregate functions
- Concepts: ch10-aggregate-functions
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: This is a straightforward and correct test of aggregate functions over groups of rows. Only option B fits the total-by-region requirement. Explanation is accurate, and difficulty 2 is suitable.

### Prompt

A sales manager wants the total revenue for each region, ignoring individual orders once they have been grouped by region. Which SQL feature is the best fit?

| Option | Text |
| --- | --- |
| A | Scalar functions |
| B ✓ | Aggregate functions |
| C | Collation |
| D | Data types |

Correct answer: **B — Aggregate functions**

Explanation: Aggregate functions summarize multiple rows, often after grouping them by a column such as region. A total revenue calculation combines values across a set, so it is an aggregate task rather than a scalar transformation.

Review concept: Aggregate functions summarize sets or groups of rows, with grouping and filtering rules controlling the scope.

Source: [SQL — Scalar and aggregate functions](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=223)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-8f4bf742-de4-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 12. ch10-ai-8f4bf742-de4-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Scalar and aggregate functions
- Concepts: ch10-scalar-functions
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: The query is valid and the keyed answer accurately describes row-preserving scalar transformations via UPPER and ROUND. Other options are clearly incorrect without being absurd. Explanation is correct and instructional. Difficulty 3 is credible.

### Prompt

What does the following query do?

```sql
SELECT order_id,
       UPPER(customer_name) AS customer_name_upper,
       ROUND(order_total, 2) AS order_total_rounded
FROM orders;
```

| Option | Text |
| --- | --- |
| A ✓ | It returns one row per order and transforms two column values in each row. |
| B | It returns one row per customer with summed order totals. |
| C | It changes the stored values in the orders table. |
| D | It filters out rows where order_total is not numeric. |

Correct answer: **A — It returns one row per order and transforms two column values in each row.**

Explanation: UPPER and ROUND are scalar functions because they act on individual values in each row. The query keeps the same row-level result set and adds transformed output columns; it does not group, update, or filter the table.

Review concept: Scalar functions transform one value per evaluated row, including text, numeric, and date-time values.

Source: [SQL — Scalar and aggregate functions](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=223)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-8f4bf742-de4-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 13. ch10-ai-8f4bf742-de4-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Scalar and aggregate functions
- Concepts: ch10-aggregate-functions
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The question correctly isolates the essential feature needed to compute department averages: aggregate functions. Mention of GROUP BY and HAVING supports the scenario without creating ambiguity. Explanation is accurate. Difficulty 3 fits.

### Prompt

A finance team needs a report with one row per department and the average payroll for that department, typically using `GROUP BY` and then `HAVING` to keep only averages above 50000. Which SQL feature is essential for computing the department average?

| Option | Text |
| --- | --- |
| A | Scalar functions |
| B ✓ | Aggregate functions |
| C | Character collation |
| D | Embedded SQL |

Correct answer: **B — Aggregate functions**

Explanation: `AVG` is an aggregate function because it summarizes multiple rows within each department group into one value. `HAVING` is the clause typically used to filter grouped results, but the average itself is computed by an aggregate function, not by a scalar function, collation rule, or embedded SQL.

Review concept: Aggregate functions summarize sets or groups of rows, with grouping and filtering rules controlling the scope.

Source: [SQL — Scalar and aggregate functions](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=223)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-8f4bf742-de4-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 14. ch10-ai-94be8578-a12-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Structured query language
- Concepts: ch10-sql-standard
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: The item accurately characterizes SQL as a standardized relational language for multiple tasks. Option A is uniquely correct, distractors are plausible enough, and the explanation is sound. Difficulty 1 is appropriate.

### Prompt

A company wants one query language that can be used across different relational database products for defining and retrieving data. Which statement best matches SQL?

| Option | Text |
| --- | --- |
| A ✓ | It is a standardized language for relational data definition, querying, and control. |
| B | It is a proprietary language that only works in one vendor's database. |
| C | It is a graphical tool used only for designing tables. |
| D | It is a file format for moving data between databases. |

Correct answer: **A — It is a standardized language for relational data definition, querying, and control.**

Explanation: SQL is a standard language for working with relational data. It supports defining structures, querying data, and controlling access and other database operations across products that implement the standard.

Review concept: Recognize SQL as a standardized relational language used for multiple database tasks.

Source: [SQL — Structured query language](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=214)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-94be8578-a12-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 15. ch10-ai-94be8578-a12-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Structured query language
- Concepts: ch10-set-processing
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: The query meaning is correctly interpreted: grouping by customer_id and summing amount yields one row per customer with a total. Only option A is defensible. Explanation is accurate and educational. Difficulty 2 is appropriate.

### Prompt

A table Orders has columns order_id, customer_id, and amount. What does the following query do?

```sql
SELECT customer_id, SUM(amount)
FROM Orders
GROUP BY customer_id;
```

| Option | Text |
| --- | --- |
| A ✓ | Returns one row per customer with the total order amount for that customer. |
| B | Returns one row per order with the average amount across all orders. |
| C | Returns only the largest order for each customer. |
| D | Deletes orders after calculating totals. |

Correct answer: **A — Returns one row per customer with the total order amount for that customer.**

Explanation: The query works on the set of rows in Orders and groups them by customer_id. SUM(amount) then produces a total for each group, so the result has one row per customer with their combined order amount.

Review concept: Interpret SQL as set-oriented processing with grouping over rows.

Source: [SQL — Structured query language](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=214)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-94be8578-a12-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 16. ch10-ai-94be8578-a12-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Structured query language
- Concepts: ch10-set-processing
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: Option A is the only query that fully meets the requirement: products with at least one August 2026 sale plus total quantity per product. The explanation correctly identifies why B and C are incomplete and why D is flawed under standard SQL grouping rules. The date assumption is stated. Difficulty 3 is credible.

### Prompt

A retailer needs a report of all products that had at least one sale during August 2026, along with the total quantity sold per product in that month. The database has `Product(product_id, name)` and `Sale(product_id, qty, sale_date)`. Assume `sale_date` is stored as a date. Which query best matches the requirement in a set-oriented way?

| Option | Text |
| --- | --- |
| A ✓ | SELECT p.product_id, p.name, SUM(s.qty) AS total_qty<br>FROM Product p<br>JOIN Sale s ON s.product_id = p.product_id<br>WHERE s.sale_date >= DATE '2026-08-01'<br>  AND s.sale_date < DATE '2026-09-01'<br>GROUP BY p.product_id, p.name; |
| B | SELECT p.product_id, p.name, s.qty<br>FROM Product p<br>JOIN Sale s ON s.product_id = p.product_id<br>WHERE s.sale_date >= DATE '2026-08-01'<br>  AND s.sale_date < DATE '2026-09-01'; |
| C | SELECT p.product_id, p.name<br>FROM Product p<br>WHERE EXISTS (<br>  SELECT 1<br>  FROM Sale s<br>  WHERE s.product_id = p.product_id<br>    AND s.sale_date >= DATE '2026-08-01'<br>    AND s.sale_date < DATE '2026-09-01'<br>); |
| D | SELECT p.product_id, p.name, SUM(s.qty) AS total_qty<br>FROM Product p<br>LEFT JOIN Sale s ON s.product_id = p.product_id<br>WHERE s.sale_date >= DATE '2026-08-01'<br>  AND s.sale_date < DATE '2026-09-01'<br>GROUP BY p.product_id; |

Correct answer: **A — SELECT p.product_id, p.name, SUM(s.qty) AS total_qty
FROM Product p
JOIN Sale s ON s.product_id = p.product_id
WHERE s.sale_date >= DATE '2026-08-01'
  AND s.sale_date < DATE '2026-09-01'
GROUP BY p.product_id, p.name;**

Explanation: Option A correctly joins products to their August 2026 sales, restricts rows to that month with a bounded date range, and groups by product so each product appears once with its total quantity sold. B lists individual sale rows instead of totals, C finds products with sales but does not return the total quantity, and D is not the best answer because it selects `p.name` without grouping by it.

Review concept: Use joins, filtering, and grouping to express a result over sets of rows.

Source: [SQL — Structured query language](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=214)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-94be8578-a12-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 17. ch10-ai-94be8578-a12-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Structured query language
- Concepts: ch10-sql-standard, ch10-set-processing
- Kind/difficulty: concept, 3/5
- Automated audit: **pass**

Audit rationale: The item correctly tests SQL’s declarative, set-oriented nature against procedural row navigation. Option A is uniquely correct. Distractors are clearly wrong but still on-topic, and the explanation teaches the key contrast. Difficulty 3 is reasonable.

### Prompt

Which statement best explains why SQL is often preferred over procedural row-by-row navigation for many database tasks?

| Option | Text |
| --- | --- |
| A ✓ | SQL lets you state the desired result on sets of rows, while the database decides how to process them. |
| B | SQL requires the programmer to manually visit each row in key order. |
| C | SQL can only be used after exporting data into spreadsheets. |
| D | SQL works only for inserting rows, not for querying them. |

Correct answer: **A — SQL lets you state the desired result on sets of rows, while the database decides how to process them.**

Explanation: SQL is declarative and set-oriented: you describe what result you want, not the step-by-step procedure for visiting each row. That makes it different from procedural row-by-row processing.

Review concept: Connect SQL’s standard declarative nature with set-oriented processing.

Source: [SQL — Structured query language](https://www.richardtwatson.com/open/Reader/_book/Data-Management.pdf#page=214)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-94be8578-a12-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 18. ch10-ai-cd87e56b-3cc-1

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Views, indexes, and the catalog
- Concepts: ch10-views
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The question accurately defines a view as a named virtual table derived from a query. Only option A fits. Distractors are plausible and distinct. Explanation is correct and aligned to the chapter concept. Difficulty 2 is suitable.

### Prompt

A reporting team wants a named virtual table derived from a query that shows only active customers. Which database object best fits that need?

| Option | Text |
| --- | --- |
| A ✓ | A view |
| B | An index |
| C | A catalog entry |
| D | A privilege |

Correct answer: **A — A view**

Explanation: A view is a named virtual table defined by a query, so it can present only the rows and columns needed, such as active customers. An index is for access speed, the catalog stores metadata about database objects, and a privilege controls access rather than defining a query-derived table.

Review concept: Views

Source: [SQL — Views, indexes, and the catalog](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-cd87e56b-3cc-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 19. ch10-ai-cd87e56b-3cc-2

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Views, indexes, and the catalog
- Concepts: ch10-indexes
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: The SQL statement and its interpretation are correct: a unique composite index can improve retrieval on the indexed columns and enforce uniqueness of the column pair. Only option A is defensible. Explanation properly notes the leading-column behavior. Difficulty 3 is appropriate.

### Prompt

A table `Orders(order_id, customer_id, order_date, total_amount)` already contains many rows. Which statement about this index is most accurate?

```sql
CREATE UNIQUE INDEX idx_orders_customer_date
ON Orders(customer_id, order_date);
```

| Option | Text |
| --- | --- |
| A ✓ | It can speed retrieval for searches that use the indexed columns, especially `customer_id` and `customer_id` with `order_date`, and it enforces uniqueness of each `(customer_id, order_date)` pair. |
| B | It creates a virtual table that shows only one row for each customer and date. |
| C | It guarantees that every row in `Orders` is unique in all columns. |
| D | It enforces that `total_amount` must be greater than zero. |

Correct answer: **A — It can speed retrieval for searches that use the indexed columns, especially `customer_id` and `customer_id` with `order_date`, and it enforces uniqueness of each `(customer_id, order_date)` pair.**

Explanation: A composite index on `(customer_id, order_date)` can improve access for queries that use those indexed columns, particularly ones beginning with the leading column `customer_id`. Because the index is `UNIQUE`, the database also enforces that no two rows can have the same combination of `customer_id` and `order_date`. It does not create a view, it does not guarantee full-row uniqueness across all columns, and it does not enforce any rule on `total_amount`.

Review concept: Indexes

Source: [SQL — Views, indexes, and the catalog](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-cd87e56b-3cc-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 20. ch10-ai-cd87e56b-3cc-3

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Views, indexes, and the catalog
- Concepts: ch10-catalog
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The item correctly identifies the database catalog as the source of metadata about tables and columns. Only option A matches. Explanation is accurate and useful. Difficulty 2 is appropriate.

### Prompt

An administrator needs to find metadata about tables and columns in a database, such as object names and column definitions. Where should they look?

| Option | Text |
| --- | --- |
| A ✓ | The database catalog |
| B | A view definition |
| C | An aggregate query result |
| D | A user privilege list only |

Correct answer: **A — The database catalog**

Explanation: The catalog is the metadata layer that describes database objects such as tables and columns. It is used for administrative queries about the structure of the database, not for the data rows themselves.

Review concept: Database catalog

Source: [SQL — Views, indexes, and the catalog](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-cd87e56b-3cc-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 21. ch10-ai-cd87e56b-3cc-4

- Human decision: [X] Approve  [ ] Needs edit  [ ] Reject
- Section: Views, indexes, and the catalog
- Concepts: ch10-catalog
- Kind/difficulty: sql-scenario, 4/5
- Automated audit: **pass**

Audit rationale: This is a clear metadata-lookup question, and the database catalog is the only correct answer. Distractors are plausible and non-overlapping. Explanation is accurate, and difficulty 4 is somewhat high but still defensible for learners distinguishing object roles.

### Prompt

An administrator wants to verify the names and data types of columns in the base table `Orders`. Which resource is designed for this kind of database metadata lookup?

| Option | Text |
| --- | --- |
| A ✓ | The database catalog |
| B | An index on `Orders` |
| C | A `GRANT` statement |
| D | An embedded SQL program |

Correct answer: **A — The database catalog**

Explanation: The database catalog stores metadata about database objects such as tables and columns, so it is the appropriate place to check column names and data types. An index helps access rows efficiently, `GRANT` manages privileges, and embedded SQL concerns how SQL is used inside a host program rather than where metadata is stored.

Review concept: Database catalog

Source: [SQL — Views, indexes, and the catalog](https://www.richardtwatson.com/open/Reader/answers/chapter10.html)

Reviewer notes:

- 

Approval command after review:

```sh
npm run questions:approve -- --id ch10-ai-cd87e56b-3cc-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

