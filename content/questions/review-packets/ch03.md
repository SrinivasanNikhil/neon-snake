# Chapter 3 question review packet

Candidates: 20. Automated semantic audit: gpt-5.4 at 2026-08-13T16:20:21.736Z.

> The automated audit is advisory. A person must verify the source, SQL behavior, answer uniqueness, wording, and explanation before approval.

## Chapter checklist

- [ ] Every question was read in full.
- [ ] Every SQL result or constraint claim was independently checked.
- [ ] Every correct answer is uniquely defensible.
- [ ] Every source section supports the tested concept.
- [ ] Wording is original, accessible, and not copied from the book.
- [ ] The chapter has acceptable section and difficulty coverage.

## 1. ch03-ai-a68f1455-2bc-1

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Creating a single-table database
- Concepts: ch03-model-to-table
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Accurate direct mapping from single-entity model to one table. One answer is clearly correct, distractors are plausible structural confusions, and the explanation teaches the mapping cleanly.

### Prompt

In a one-entity design, a company turns a single business object into one relational table. What is the best mapping from the model to the table structure?

| Option | Text                                                                                                                |
| ------ | ------------------------------------------------------------------------------------------------------------------- |
| A ✓    | The entity becomes a table, the attributes become columns, and the identifier becomes the primary key               |
| B      | The entity becomes a column, the attributes become rows, and the identifier becomes a foreign key                   |
| C      | The entity becomes an index, the attributes become constraints, and the identifier becomes a view                   |
| D      | The entity becomes a trigger, the attributes become stored procedures, and the identifier becomes a unique database |

Correct answer: **A — The entity becomes a table, the attributes become columns, and the identifier becomes the primary key**

Explanation: In a direct single-entity mapping, each entity type is represented by one table. The entity’s attributes are stored as columns, and the attribute or combination of attributes that uniquely identifies each instance is used as the table’s primary key.

Review concept: Mapping a model to a table

Source: [The Single Entity — Creating a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#creating-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-a68f1455-2bc-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 2. ch03-ai-a68f1455-2bc-2

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Creating a single-table database
- Concepts: ch03-create-integrity
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: Entity integrity is correctly characterized as uniqueness plus non-nullability of the primary key. The distractors are plausible misconceptions and difficulty is credible.

### Prompt

A table is being created for employee records. Which definition best satisfies entity integrity for the table's primary key?

| Option | Text                                                                          |
| ------ | ----------------------------------------------------------------------------- |
| A      | The key column may contain duplicates if the names are different              |
| B ✓    | The key column must uniquely identify each row and must never be NULL         |
| C      | The key column should be stored as text so it can hold any value              |
| D      | The key column only needs to be unique when rows are inserted, not after that |

Correct answer: **B — The key column must uniquely identify each row and must never be NULL**

Explanation: Entity integrity means every row can be identified without ambiguity. A primary key must therefore be unique and non-null for each row.

Review concept: CREATE and entity integrity

Source: [The Single Entity — Creating a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#creating-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-a68f1455-2bc-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 3. ch03-ai-a68f1455-2bc-3

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Creating a single-table database
- Concepts: ch03-create-integrity
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **needs-edit** — missing-assumption, poor-explanation

Audit rationale: The keyed answer is defensible only because the prompt says the table is otherwise empty, but the explanation mentions only NULL and omits the equally important uniqueness side of entity integrity. As written, the teaching value is narrower than the stem’s concept. The item would be stronger if it explicitly contrasted NULL and duplicate primary-key violations.

Suggested fix: Revise the explanation to note that primary keys must be both unique and non-NULL, and that with an otherwise empty table the only listed violating row is the one with NULL `product_id`. Optionally add a duplicate-key option instead of one of the ordinary valid rows to reinforce uniqueness.

### Prompt

A table is defined as follows in MySQL:

```sql
CREATE TABLE Product (
  product_id INT,
  product_name VARCHAR(50),
  price DECIMAL(8,2),
  PRIMARY KEY (product_id)
);
```

Which inserted row would violate the table's entity integrity rules? Assume the table is otherwise empty.

| Option | Text                    |
| ------ | ----------------------- |
| A      | (101, 'Pen', 1.25)      |
| B      | (102, 'Notebook', 4.50) |
| C ✓    | (NULL, 'Stapler', 8.99) |
| D      | (103, 'Marker', 2.00)   |

Correct answer: **C — (NULL, 'Stapler', 8.99)**

Explanation: The primary key column cannot be NULL. A row with a NULL product_id violates entity integrity, even if all other values are valid.

Review concept: CREATE and entity integrity

Source: [The Single Entity — Creating a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#creating-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-a68f1455-2bc-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 4. ch03-ai-a68f1455-2bc-4

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Creating a single-table database
- Concepts: ch03-model-to-table
- Kind/difficulty: sql-scenario, 4/5
- Automated audit: **pass**

Audit rationale: Option A correctly maps the entity and identifier into a single table with an appropriate primary key. The distractors are plausible and expose misunderstandings about primary keys, foreign keys, and indexes.

### Prompt

A small clinic models only one entity, Patient. The business rules are:

- each patient is identified by one patient_number
- each patient has a full_name and a date_of_birth
- the design must store the entity in one table

Which MySQL table definition best matches that model?

| Option | Text                                                                                                                                                                        |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A ✓    | CREATE TABLE Patient (<br> patient_number INT PRIMARY KEY,<br> full_name VARCHAR(80),<br> date_of_birth DATE<br>);                                                          |
| B      | CREATE TABLE Patient (<br> full_name VARCHAR(80) PRIMARY KEY,<br> patient_number INT,<br> date_of_birth DATE<br>);                                                          |
| C      | CREATE TABLE Patient (<br> patient_number INT,<br> full_name VARCHAR(80),<br> date_of_birth DATE,<br> FOREIGN KEY (patient_number) REFERENCES Patient(patient_number)<br>); |
| D      | CREATE TABLE Patient (<br> patient_number INT,<br> full_name VARCHAR(80),<br> date_of_birth DATE,<br> INDEX(patient_number)<br>);                                           |

Correct answer: **A — CREATE TABLE Patient (
patient_number INT PRIMARY KEY,
full_name VARCHAR(80),
date_of_birth DATE
);**

Explanation: The single-entity model maps directly to one table. The identifier should become the primary key, and the remaining attributes should become regular columns. Option A does this correctly.

Review concept: Mapping a model to a table

Source: [The Single Entity — Creating a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#creating-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-a68f1455-2bc-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 5. ch03-ai-ff4d8a8e-3d7-2

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Creating a single-table database
- Concepts: ch03-model-to-table, ch03-create-integrity
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The item correctly combines model-to-table mapping with entity integrity. Exactly one answer is defensible, and the explanation clearly covers both column mapping and primary-key requirements.

### Prompt

A business model has one entity, Supplier, with attributes SupplierID, SupplierName, and Phone. Which table design best matches the model and preserves entity integrity?

| Option | Text                                                                                                          |
| ------ | ------------------------------------------------------------------------------------------------------------- |
| A ✓    | Create a Supplier table with columns SupplierID, SupplierName, and Phone, and make SupplierID the primary key |
| B      | Create separate tables for SupplierName and Phone, and leave SupplierID out because it duplicates data        |
| C      | Create a Supplier table and allow SupplierID to be NULL as long as SupplierName is unique                     |
| D      | Store SupplierID in a text column but do not define any primary key                                           |

Correct answer: **A — Create a Supplier table with columns SupplierID, SupplierName, and Phone, and make SupplierID the primary key**

Explanation: A single entity maps to one table, its attributes map to columns, and the identifier maps to the primary key. A primary key must uniquely identify each row and cannot be NULL, so SupplierID should be the primary key.

Review concept: Translate a single-entity model into one table and enforce entity integrity.

Source: [The Single Entity — Creating a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#creating-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-ff4d8a8e-3d7-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 6. ch03-ai-6c6bf6d2-6f5-1

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: DELETE and UPDATE
- Concepts: ch03-update-delete
- Kind/difficulty: sql-scenario, 2/5
- Automated audit: **pass**

Audit rationale: SQL is correct, exactly one option satisfies the stated requirement, and distractors map to common DML confusions. Difficulty is suitable for introductory UPDATE usage.

### Prompt

A table named `subscriptions` has columns `sub_id` (primary key), `status`, and `renewal_date`. You want to change only the row for subscription 104 so that its `status` becomes `'paused'`. Which statement does that safely?

| Option | Text                                                               |
| ------ | ------------------------------------------------------------------ |
| A ✓    | UPDATE subscriptions SET status = 'paused' WHERE sub_id = 104;     |
| B      | UPDATE subscriptions SET status = 'paused';                        |
| C      | DELETE FROM subscriptions WHERE sub_id = 104;                      |
| D      | INSERT INTO subscriptions (sub_id, status) VALUES (104, 'paused'); |

Correct answer: **A — UPDATE subscriptions SET status = 'paused' WHERE sub_id = 104;**

Explanation: This updates exactly the row identified by `sub_id = 104`. The other UPDATE form would affect every row because it lacks a WHERE clause, while DELETE removes the row and INSERT adds a new row instead of changing the existing one.

Review concept: UPDATE changes selected rows and DELETE removes selected rows; omitting an appropriate WHERE condition can affect every row.

Source: [The Single Entity — DELETE and UPDATE](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#delete)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-6c6bf6d2-6f5-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 7. ch03-ai-6c6bf6d2-6f5-2

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: DELETE and UPDATE
- Concepts: ch03-update-delete
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Clear and accurate concept check on UPDATE without WHERE. One answer is plainly correct, and the distractors reflect common beginner errors.

### Prompt

What is the main risk of running an UPDATE statement without a WHERE clause?

| Option | Text                                    |
| ------ | --------------------------------------- |
| A ✓    | It may change every row in the table.   |
| B      | It can only change the primary key.     |
| C      | It automatically deletes matching rows. |
| D      | It fails unless the table is empty.     |

Correct answer: **A — It may change every row in the table.**

Explanation: Without a WHERE clause, UPDATE applies to all rows in the table. It does not delete rows or require the table to be empty.

Review concept: UPDATE changes selected rows and DELETE removes selected rows; omitting an appropriate WHERE condition can affect every row.

Source: [The Single Entity — DELETE and UPDATE](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#delete)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-6c6bf6d2-6f5-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 8. ch03-ai-6c6bf6d2-6f5-3

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: DELETE and UPDATE
- Concepts: ch03-update-delete
- Kind/difficulty: sql-reading, 3/5
- Automated audit: **pass**

Audit rationale: The DELETE statement is interpreted correctly, with a concrete before-state and one unambiguous result. Explanation is accurate and instructional, and the level is appropriate.

### Prompt

Assume `orders(order_id, ship_status)` contains these rows: (1,'open'), (2,'open'), (3,'closed'). What is the result of running this statement?

```sql
DELETE FROM orders WHERE ship_status = 'open';
```

| Option | Text                                                      |
| ------ | --------------------------------------------------------- |
| A ✓    | Rows 1 and 2 are removed; row 3 remains.                  |
| B      | Only row 3 is removed.                                    |
| C      | All rows are changed to `ship_status = 'open'`.           |
| D      | The statement adds a new row with `ship_status = 'open'`. |

Correct answer: **A — Rows 1 and 2 are removed; row 3 remains.**

Explanation: DELETE removes rows that satisfy the WHERE condition. Here, rows 1 and 2 match `ship_status = 'open'`, so they are removed and the closed order remains.

Review concept: UPDATE changes selected rows and DELETE removes selected rows; omitting an appropriate WHERE condition can affect every row.

Source: [The Single Entity — DELETE and UPDATE](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#delete)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-6c6bf6d2-6f5-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 9. ch03-ai-6c6bf6d2-6f5-4

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: DELETE and UPDATE
- Concepts: ch03-update-delete
- Kind/difficulty: sql-scenario, 2/5
- Automated audit: **pass**

Audit rationale: The statement in option A precisely matches the business rule by requiring both predicates. Other options represent realistic mistakes, and the explanation clearly contrasts them.

### Prompt

In `customers(customer_id, active_flag, city)`, `customer_id` uniquely identifies each customer. A manager wants to mark customer 88 as inactive, but only if that customer's row has `city = 'Boston'`. Which statement does that update?

| Option | Text                                                                             |
| ------ | -------------------------------------------------------------------------------- |
| A ✓    | UPDATE customers SET active_flag = 0 WHERE customer_id = 88 AND city = 'Boston'; |
| B      | UPDATE customers SET active_flag = 0 WHERE customer_id = 88;                     |
| C      | UPDATE customers SET active_flag = 0 WHERE city = 'Boston';                      |
| D      | DELETE FROM customers WHERE customer_id = 88 AND city = 'Boston';                |

Correct answer: **A — UPDATE customers SET active_flag = 0 WHERE customer_id = 88 AND city = 'Boston';**

Explanation: The task requires both conditions: the row must be for customer 88 and must have `city = 'Boston'`. Option A updates only when both are true. Option B ignores the city requirement, option C could affect many Boston customers, and option D removes the row instead of updating it.

Review concept: Maintaining rows

Source: [The Single Entity — DELETE and UPDATE](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#delete)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-6c6bf6d2-6f5-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 10. ch03-ai-ff4d8a8e-3d7-5

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: DELETE and UPDATE
- Concepts: ch03-update-delete
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **needs-edit** — sql-error, weak-distractor

Audit rationale: Option D is syntactically invalid SQL (`UPDATE ... WHERE ... SET`), which makes it a weak distractor because it can be dismissed on syntax rather than concept. The concept being tested is safe row-targeted UPDATE, so distractors should be incorrect but syntactically plausible.

Suggested fix: Replace option D with a valid but conceptually wrong statement, such as `UPDATE Inventory SET Quantity = 42 WHERE ItemID = 42;` or `UPDATE Inventory SET Quantity = Quantity - 1 WHERE ItemName = '42';`, and update the explanation if needed.

### Prompt

A table named Inventory has columns ItemID, ItemName, and Quantity. You want to reduce the quantity by 1 only for the row where ItemID = 42. Which statement does that safely?

| Option | Text                                                            |
| ------ | --------------------------------------------------------------- |
| A ✓    | UPDATE Inventory SET Quantity = Quantity - 1 WHERE ItemID = 42; |
| B      | UPDATE Inventory SET Quantity = Quantity - 1;                   |
| C      | DELETE FROM Inventory WHERE ItemID = 42;                        |
| D      | UPDATE Inventory WHERE ItemID = 42 SET Quantity = Quantity - 1; |

Correct answer: **A — UPDATE Inventory SET Quantity = Quantity - 1 WHERE ItemID = 42;**

Explanation: UPDATE changes existing rows, and the WHERE clause limits the change to the intended row. Without WHERE, every row would be affected. DELETE would remove the row instead of adjusting quantity.

Review concept: Use UPDATE with a WHERE clause to modify only the intended row.

Source: [The Single Entity — DELETE and UPDATE](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#delete)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-ff4d8a8e-3d7-5 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 11. ch03-ai-2bc1cb18-c32-1

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Modeling a single-entity database
- Concepts: ch03-entity-attributes
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Accurate concept question on attributes with one clearly defensible answer. Distractors are plausible category confusions, and the explanation is concise but adequate for the level.

### Prompt

A small company wants to keep one table of its customers. Which statement best describes an attribute in that model?

| Option | Text                                                             |
| ------ | ---------------------------------------------------------------- |
| A ✓    | A fact stored about each customer, such as email or phone number |
| B      | A rule that controls which users can view the table              |
| C      | A saved query used to display customer rows                      |
| D      | A physical file that holds the table on disk                     |

Correct answer: **A — A fact stored about each customer, such as email or phone number**

Explanation: An attribute is a piece of information recorded about each entity instance. In a customer model, values like email or phone number are attributes because they describe each customer.

Review concept: Entities and attributes

Source: [The Single Entity — Modeling a single-entity database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#modeling-a-single-entity-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-2bc1cb18-c32-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 12. ch03-ai-2bc1cb18-c32-2

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Modeling a single-entity database
- Concepts: ch03-identifiers
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The item correctly defines an identifier as a unique value or combination of values. Alternatives are clearly wrong but still plausible enough for beginners, and the difficulty is appropriate.

### Prompt

A library system records one row per book copy. It needs a way to distinguish every row even when two copies share the same title and author. Which design best fits that need?

| Option | Text                                                                |
| ------ | ------------------------------------------------------------------- |
| A ✓    | Use a value, or combination of values, that is unique for every row |
| B      | Use any descriptive column, even if some rows repeat it             |
| C      | Use the column with the longest text values                         |
| D      | Use a column only if it can contain duplicate values                |

Correct answer: **A — Use a value, or combination of values, that is unique for every row**

Explanation: An identifier must uniquely distinguish each entity instance. If one column is not enough, a combination of columns can serve as the identifier as long as the combination is unique for every row.

Review concept: Identifiers

Source: [The Single Entity — Modeling a single-entity database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#modeling-a-single-entity-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-2bc1cb18-c32-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 13. ch03-ai-2bc1cb18-c32-3

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Modeling a single-entity database
- Concepts: ch03-identifiers
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: Good composite-key question with correct MySQL syntax and a single defensible answer. It fits the chapter’s identifier coverage well and the explanation properly links the business rule to a composite primary key.

### Prompt

A table named Product has columns (product_code, warehouse_code, description, price). Business rules say that the same product_code may appear in more than one warehouse, but the pair (product_code, warehouse_code) must identify each row exactly once. Which key definition best matches that rule in MySQL?

| Option | Text                                       |
| ------ | ------------------------------------------ |
| A ✓    | PRIMARY KEY (product_code, warehouse_code) |
| B      | PRIMARY KEY (product_code)                 |
| C      | PRIMARY KEY (warehouse_code)               |
| D      | UNIQUE (description, price)                |

Correct answer: **A — PRIMARY KEY (product_code, warehouse_code)**

Explanation: The rule says no single column is enough, but the combination of product_code and warehouse_code must be unique for each row. A composite primary key on those two columns matches that requirement.

Review concept: Identifiers

Source: [The Single Entity — Modeling a single-entity database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#modeling-a-single-entity-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-2bc1cb18-c32-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 14. ch03-ai-2bc1cb18-c32-4

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Modeling a single-entity database
- Concepts: ch03-entity-attributes, ch03-identifiers
- Kind/difficulty: concept, 2/5
- Automated audit: **pass**

Audit rationale: The model-level description of an identifier is correct and well matched to the concept. Distractors are plausible misconceptions, and the explanation distinguishes identification from search, volatility, and aggregates.

### Prompt

A startup is designing one table for its subscriptions. Each row represents one subscription. Which statement is the best model-level description of the table's identifier?

| Option | Text                                                                                                              |
| ------ | ----------------------------------------------------------------------------------------------------------------- |
| A ✓    | It is the attribute, or set of attributes, that uniquely separates one subscription from every other subscription |
| B      | It is any attribute that happens to be frequently searched                                                        |
| C      | It is the attribute that changes most often over time                                                             |
| D      | It is a column that stores the total number of subscriptions                                                      |

Correct answer: **A — It is the attribute, or set of attributes, that uniquely separates one subscription from every other subscription**

Explanation: An identifier is the attribute or combination of attributes that uniquely distinguishes each instance of the entity. The other choices describe query convenience, volatility, or aggregate information rather than identification.

Review concept: Identifiers

Source: [The Single Entity — Modeling a single-entity database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#modeling-a-single-entity-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-2bc1cb18-c32-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 15. ch03-ai-ff4d8a8e-3d7-1

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Modeling a single-entity database
- Concepts: ch03-entity-attributes
- Kind/difficulty: concept, 1/5
- Automated audit: **pass**

Audit rationale: Straightforward and accurate attribute-identification item. One answer is correct, distractors are plausible, and the explanation is suitable for the introductory level.

### Prompt

A company is designing a table for its fleet of delivery scooters. Which choice best describes an attribute in the model?

| Option | Text                                                                      |
| ------ | ------------------------------------------------------------------------- |
| A ✓    | A fact recorded about each scooter, such as its color or battery capacity |
| B      | A rule that says every scooter must have a unique serial number           |
| C      | A collection of scooters that share the same manufacturer                 |
| D      | A query that sorts scooters by battery level                              |

Correct answer: **A — A fact recorded about each scooter, such as its color or battery capacity**

Explanation: An attribute stores a fact about each instance of an entity. In this case, color or battery capacity are examples of facts that describe each scooter.

Review concept: Identify entities and attributes in a simple single-entity model.

Source: [The Single Entity — Modeling a single-entity database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#modeling-a-single-entity-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-ff4d8a8e-3d7-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 16. ch03-ai-1cde71c7-404-1

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Querying a single-table database
- Concepts: ch03-select-filter
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: Stem, SQL, options, key, and explanation all align. Exactly one answer is defensible, distractors are plausible, and the difficulty is appropriate for basic projection/restriction reading.

### Prompt

A table `product` has columns `product_name`, `category`, and `price`. What does this query return?

```sql
SELECT product_name
FROM product
WHERE category = 'Books' AND price >= 20;
```

| Option | Text                                                                                             |
| ------ | ------------------------------------------------------------------------------------------------ |
| A ✓    | Only `product_name` values for rows where `category` is `Books` and `price` is 20 or higher.     |
| B      | All columns for rows where `category` is `Books` and `price` is 20 or higher.                    |
| C      | Only `product_name` values for rows where `category` is not `Books` and `price` is 20 or higher. |
| D      | Only `product_name` values for every row in `product`, regardless of category and price.         |

Correct answer: **A — Only `product_name` values for rows where `category` is `Books` and `price` is 20 or higher.**

Explanation: `SELECT product_name` projects just the `product_name` column. The `WHERE` clause restricts rows to those where both conditions are true: `category = 'Books'` and `price >= 20`. So the result contains only product names from qualifying rows, not all columns or all rows.

Review concept: Projection and restriction

Source: [The Single Entity — Querying a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#querying-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-1cde71c7-404-1 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 17. ch03-ai-1cde71c7-404-2

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Querying a single-table database
- Concepts: ch03-order-derived
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The query correctly demonstrates a derived column and ordering by that derived alias. One correct answer is clear, distractors are plausible, and the explanation teaches both computed output and non-persistence of SELECT expressions.

### Prompt

A table `invoice` has columns `invoice_id`, `subtotal`, and `tax_rate`. What does this query do?

```sql
SELECT invoice_id, subtotal * (1 + tax_rate) AS total_charge
FROM invoice
ORDER BY total_charge DESC;
```

| Option | Text                                                                                                       |
| ------ | ---------------------------------------------------------------------------------------------------------- |
| A ✓    | It returns each `invoice_id` with a calculated `total_charge`, sorted from highest total charge to lowest. |
| B      | It permanently stores `total_charge` as a new column in `invoice` and sorts by `invoice_id`.               |
| C      | It returns only invoices whose calculated total charge is above the average total charge.                  |
| D      | It sorts rows by `subtotal` descending and displays `tax_rate` as `total_charge`.                          |

Correct answer: **A — It returns each `invoice_id` with a calculated `total_charge`, sorted from highest total charge to lowest.**

Explanation: The expression `subtotal * (1 + tax_rate)` computes a derived value for each row and labels it `total_charge` in the result. `ORDER BY total_charge DESC` then sorts the output from the largest computed total to the smallest. The query reports derived values; it does not store them permanently.

Review concept: Ordering and derived values

Source: [The Single Entity — Querying a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#querying-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-1cde71c7-404-2 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 18. ch03-ai-1cde71c7-404-3

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Querying a single-table database
- Concepts: ch03-aggregate-subquery
- Kind/difficulty: sql-reading, 4/5
- Automated audit: **pass**

Audit rationale: SQL and factual interpretation are correct: the scalar subquery computes one departmental average, and the outer query compares all employees against it. Options cleanly separate common misunderstandings, and the difficulty is credible.

### Prompt

A table `employee` has columns `employee_id`, `department_id`, and `salary`. What does this query return?

```sql
SELECT employee_id, salary
FROM employee
WHERE salary > (
  SELECT AVG(salary)
  FROM employee
  WHERE department_id = 10
);
```

| Option | Text                                                                                                         |
| ------ | ------------------------------------------------------------------------------------------------------------ |
| A ✓    | Employees from any department whose salary is greater than the average salary of employees in department 10. |
| B      | Only employees in department 10 whose salary is greater than their department average.                       |
| C      | One row containing just the average salary for department 10.                                                |
| D      | Employees whose salary is greater than the highest salary in department 10.                                  |

Correct answer: **A — Employees from any department whose salary is greater than the average salary of employees in department 10.**

Explanation: The subquery returns one value: the average `salary` for rows where `department_id = 10`. The outer query compares every employee's salary to that single value. Because the outer query has no condition limiting rows to department 10, qualifying employees can come from any department.

Review concept: Aggregates and subqueries

Source: [The Single Entity — Querying a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#querying-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-1cde71c7-404-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 19. ch03-ai-ff4d8a8e-3d7-3

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Querying a single-table database
- Concepts: ch03-select-filter, ch03-order-derived
- Kind/difficulty: sql-reading, 2/5
- Automated audit: **pass**

Audit rationale: SQL behavior is correct across selection, filtering, derived value calculation, and ordering. The stem and options align well, and the explanation accurately teaches all four clauses.

### Prompt

A table named Product has columns ProductName, UnitPrice, and UnitsInStock. What does the following query return?

```sql
SELECT ProductName, UnitPrice * UnitsInStock AS InventoryValue
FROM Product
WHERE UnitsInStock >= 10
ORDER BY InventoryValue DESC;
```

| Option | Text                                                                                                                                    |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| A ✓    | Product names and computed inventory values for products with at least 10 units in stock, sorted from highest to lowest inventory value |
| B      | All products, sorted alphabetically by ProductName, with UnitPrice and UnitsInStock unchanged                                           |
| C      | Only the total inventory value for the entire table, with no product names shown                                                        |
| D      | Products with fewer than 10 units in stock, sorted from lowest to highest value                                                         |

Correct answer: **A — Product names and computed inventory values for products with at least 10 units in stock, sorted from highest to lowest inventory value**

Explanation: SELECT chooses the output columns, WHERE keeps only rows meeting the stock condition, the expression calculates a derived value, and ORDER BY sorts by that derived value in descending order.

Review concept: Read a query that filters rows and orders by a calculated column.

Source: [The Single Entity — Querying a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#querying-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-ff4d8a8e-3d7-3 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```

## 20. ch03-ai-ff4d8a8e-3d7-4

- Human decision: [X] Approve [ ] Needs edit [ ] Reject
- Section: Querying a single-table database
- Concepts: ch03-pattern-distinct
- Kind/difficulty: sql-scenario, 3/5
- Automated audit: **pass**

Audit rationale: The case-insensitive collation assumption is explicitly stated, SQL is correct, and the distractors target realistic errors involving DISTINCT placement, wrong filtered column, and exact-match confusion. Difficulty is appropriate.

### Prompt

A table `Customer` has columns `CustomerName` and `Email`. Assume string matching uses the table's default case-insensitive collation. You need one query that returns the number of distinct non-NULL email addresses used by customers whose names contain the letter `a` or `A`. Which query is best?

| Option | Text                                                                      |
| ------ | ------------------------------------------------------------------------- |
| A ✓    | SELECT COUNT(DISTINCT Email) FROM Customer WHERE CustomerName LIKE '%a%'; |
| B      | SELECT DISTINCT COUNT(Email) FROM Customer WHERE CustomerName LIKE '%a%'; |
| C      | SELECT COUNT(DISTINCT CustomerName) FROM Customer WHERE Email LIKE '%a%'; |
| D      | SELECT COUNT(DISTINCT Email) FROM Customer WHERE CustomerName = 'a';      |

Correct answer: **A — SELECT COUNT(DISTINCT Email) FROM Customer WHERE CustomerName LIKE '%a%';**

Explanation: Option A is correct because `WHERE CustomerName LIKE '%a%'` restricts the rows to customers whose names contain the letter `a` under the stated case-insensitive matching assumption, and `COUNT(DISTINCT Email)` counts unique email values while ignoring NULLs. Option B is incorrect because `DISTINCT` is applied to the single aggregate result, not to the email values being counted. Option C filters the wrong column and counts distinct customer names instead of distinct email addresses. Option D tests for a name exactly equal to `a`, not a name that contains `a`.

Review concept: Patterns and duplicate removal

Source: [The Single Entity — Querying a single-table database](https://www.richardtwatson.com/open/Reader/_book/203-singleentity.html#querying-a-single-table-database)

Reviewer notes:

-

Approval command after review:

```sh
npm run questions:approve -- --id ch03-ai-ff4d8a8e-3d7-4 --reviewer "YOUR NAME" --notes "Checked correctness, ambiguity, source fit, and paraphrasing." --confirm-human-review
```
